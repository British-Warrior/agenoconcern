---
phase: 21-interactive-component-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/components/circles/CircleFormationModal.tsx
  - packages/web/src/components/circles/AddMemberModal.tsx
  - packages/web/src/pages/admin/InstitutionManagement.tsx
  - packages/web/src/components/ui/ConsentBanner.tsx
  - packages/web/src/components/challenges/ManageChallengeRow.tsx
autonomous: true

must_haves:
  truths:
    - "Opening CircleFormationModal with keyboard traps focus inside — Tab cycles within the modal and does not reach background content"
    - "Opening AddMemberModal with keyboard traps focus inside — Tab cycles within the modal and does not reach background content"
    - "Opening ConfirmDialog with keyboard traps focus inside — Tab cycles within the modal and does not reach background content"
    - "Pressing Escape closes any open modal and returns focus to the triggering button"
    - "The ConsentBanner 'Manage Preferences' button has aria-expanded reflecting its open/closed state"
    - "The ManageChallengeRow 'View Interests' and 'View Team Suggestions' buttons have aria-expanded reflecting their open/closed state"
  artifacts:
    - path: "packages/web/src/components/circles/CircleFormationModal.tsx"
      provides: "Modal-wrapped circle formation dialog"
      contains: "<Modal"
    - path: "packages/web/src/components/circles/AddMemberModal.tsx"
      provides: "Modal-wrapped add member dialog"
      contains: "<Modal"
    - path: "packages/web/src/pages/admin/InstitutionManagement.tsx"
      provides: "Modal-wrapped confirm dialog"
      contains: "<Modal"
    - path: "packages/web/src/components/ui/ConsentBanner.tsx"
      provides: "aria-expanded on Manage Preferences toggle"
      contains: "aria-expanded"
    - path: "packages/web/src/components/challenges/ManageChallengeRow.tsx"
      provides: "aria-expanded on disclosure toggle buttons"
      contains: "aria-expanded"
  key_links:
    - from: "packages/web/src/components/circles/CircleFormationModal.tsx"
      to: "packages/web/src/components/ui/Modal.tsx"
      via: "import and render"
      pattern: "import.*Modal.*from.*ui/Modal"
    - from: "packages/web/src/components/circles/AddMemberModal.tsx"
      to: "packages/web/src/components/ui/Modal.tsx"
      via: "import and render"
      pattern: "import.*Modal.*from.*ui/Modal"
    - from: "packages/web/src/pages/admin/InstitutionManagement.tsx"
      to: "packages/web/src/components/ui/Modal.tsx"
      via: "import and render"
      pattern: "import.*Modal.*from.*ui/Modal"
---

<objective>
Migrate all three existing modals (CircleFormationModal, AddMemberModal, ConfirmDialog) to the shared Modal wrapper from Phase 20, and add aria-expanded to every non-Radix disclosure widget in the application.

Purpose: Keyboard users get focus trapping and Escape-close on all dialogs; screen readers announce open/closed state on all disclosure toggles. Satisfies success criteria 1, 2, and 4 from the phase goal.
Output: Five modified files — three modal migrations, two aria-expanded additions.
</objective>

<execution_context>
@C:\Users\kirk_\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\kirk_\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-focus-infrastructure/20-01-SUMMARY.md
@packages/web/src/components/ui/Modal.tsx
@packages/web/src/components/a11y/useFocusTrap.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate CircleFormationModal, AddMemberModal, and ConfirmDialog to shared Modal wrapper</name>
  <files>
    packages/web/src/components/circles/CircleFormationModal.tsx
    packages/web/src/components/circles/AddMemberModal.tsx
    packages/web/src/pages/admin/InstitutionManagement.tsx
  </files>
  <action>
For each of the three modals, the migration pattern is identical:

1. Import `{ Modal }` from `../../components/ui/Modal.js` (adjust relative path per file).
2. Remove the `if (!isOpen) return null;` early return — Modal handles this.
3. Remove the outer `<div>` that has `className="fixed inset-0 z-50..."`, `role="dialog"`, `aria-modal="true"`, `aria-labelledby="..."` — Modal provides all of these.
4. Remove the backdrop `<div>` with `onClick={onClose}` and `aria-hidden="true"` — Modal provides this.
5. Wrap the remaining inner panel content in `<Modal isOpen={isOpen} onClose={onClose} titleId="...">...</Modal>`, where titleId matches the existing `id` on the `<h2>`.
6. Remove the inner panel's wrapping `<div>` that has `className="relative bg-white rounded-..."` — Modal's panel provides equivalent styling. Keep any unique classes (like border) if needed by passing `className` prop to Modal.

**CircleFormationModal.tsx** specifics:
- Props already have `isOpen` and `onClose` — no signature change needed.
- `titleId="circle-formation-title"` (matches existing h2 id).
- The panel div has `border border-neutral-200` — add `className="border border-neutral-200"` to Modal if keeping that visual. Modal already provides `bg-white rounded-[var(--radius-lg)] shadow-xl w-full max-w-md p-6`.
- Remove the `role="dialog"`, `aria-modal="true"`, `aria-labelledby` from the outer div — Modal handles these.

**AddMemberModal.tsx** specifics:
- Props already have `isOpen` and `onClose` — no signature change needed.
- `titleId="add-member-modal-title"`.
- The `handleClose` function resets local state AND calls `onClose()` — keep this function and pass it as `onClose` to Modal so Escape and backdrop click trigger the full reset.
- Remove the early `if (!isOpen) return null;`.

**ConfirmDialog (inside InstitutionManagement.tsx)** specifics:
- This is an inline function component, not a separate file.
- Import `{ Modal }` from `../../components/ui/Modal.js` at the top of InstitutionManagement.tsx.
- ConfirmDialog does NOT have `isOpen` as a prop — it is conditionally rendered by the parent (`{confirmToggle && <ConfirmDialog ... />}`). Add `isOpen` prop to ConfirmDialogProps and always render `<ConfirmDialog>`, passing `isOpen={!!confirmToggle}`. Alternatively (simpler): keep the conditional rendering and always pass `isOpen={true}` to Modal inside ConfirmDialog, since the component only mounts when needed. Use this simpler approach.
- `titleId="confirm-dialog-title"`.
- `onCancel` prop serves as `onClose` for Modal.

After migration, verify each modal no longer has its own `role="dialog"`, `aria-modal`, `aria-labelledby`, fixed positioning, or backdrop — all provided by Modal.
  </action>
  <verify>
Run `npx tsc --noEmit` from packages/web — zero type errors.
Grep: `grep -r "role=\"dialog\"" packages/web/src/components/circles/ packages/web/src/pages/admin/InstitutionManagement.tsx` should return NO matches (Modal.tsx provides role=dialog, not the consumers).
Grep: `grep -r "fixed inset-0" packages/web/src/components/circles/CircleFormationModal.tsx packages/web/src/components/circles/AddMemberModal.tsx` should return NO matches.
Confirm each file imports from `components/ui/Modal.js`.
  </verify>
  <done>
CircleFormationModal, AddMemberModal, and ConfirmDialog all render through the shared Modal wrapper. None contain their own role=dialog, aria-modal, backdrop, or fixed positioning. Focus trapping and Escape-close are provided by Modal for all three.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add aria-expanded to ConsentBanner and ManageChallengeRow disclosure buttons</name>
  <files>
    packages/web/src/components/ui/ConsentBanner.tsx
    packages/web/src/components/challenges/ManageChallengeRow.tsx
  </files>
  <action>
**ConsentBanner.tsx:**
The "Manage Preferences" button (line ~144) toggles `showPreferences` state. Add `aria-expanded={showPreferences}` to that button element. The button is only rendered when `!showPreferences`, so when visible it always means the section is collapsed. Set `aria-expanded="false"` on that button. Additionally, when `showPreferences` is true, the preferences panel is shown but there is no toggle button to collapse it (user clicks Save or Accept/Reject). To provide a complete disclosure pattern: change the logic so the "Manage Preferences" button is always rendered (not conditionally hidden when preferences are open) and toggles `showPreferences`. Add `aria-expanded={showPreferences}` to it. Update button text: show "Hide Preferences" when `showPreferences` is true, "Manage Preferences" when false.

Specifically:
1. Move the "Manage Preferences" button outside the `{!showPreferences && ...}` conditional — always render it.
2. Change `onClick={() => setShowPreferences(true)}` to `onClick={() => setShowPreferences(v => !v)}`.
3. Add `aria-expanded={showPreferences}` attribute.
4. Change text from static "Manage Preferences" to `{showPreferences ? "Hide Preferences" : "Manage Preferences"}`.

**ManageChallengeRow.tsx:**
Two disclosure buttons toggle `showInterests` and `showTeamSuggestions`:

1. "View Interests" / "Hide Interests" button (~line 91): Add `aria-expanded={showInterests}` to the button element. The text already toggles, which is good.

2. "View Team Suggestions" / "Hide Teams" button (~line 101): Add `aria-expanded={showTeamSuggestions}` to the button element.

No other structural changes needed — these buttons already toggle their respective state variables and conditionally render content panels.

**Note:** Radix Accordion (ChallengeAccordion.tsx) and Radix Collapsible (ChallengeSubSection.tsx) already handle aria-expanded internally — do NOT modify those files.
  </action>
  <verify>
Run `npx tsc --noEmit` from packages/web — zero type errors.
Grep: `grep -rn "aria-expanded" packages/web/src/components/ui/ConsentBanner.tsx packages/web/src/components/challenges/ManageChallengeRow.tsx` should return 3 matches (1 in ConsentBanner, 2 in ManageChallengeRow).
  </verify>
  <done>
Every non-Radix disclosure widget in the application has aria-expanded reflecting its open/closed state. ConsentBanner's "Manage Preferences" button reports expanded/collapsed. ManageChallengeRow's "View Interests" and "View Team Suggestions" buttons report expanded/collapsed. Screen readers announce the state change on toggle.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` in packages/web — zero errors
2. No file in packages/web/src (other than Modal.tsx itself) contains both `role="dialog"` and `className="fixed inset-0"` — all modals delegate to Modal wrapper
3. `grep -rn "aria-expanded" packages/web/src/` returns matches in ConsentBanner.tsx, ManageChallengeRow.tsx (and Radix components if Radix outputs it at runtime, but source-level we expect our 3 additions)
4. CircleFormationModal.tsx, AddMemberModal.tsx, InstitutionManagement.tsx all import from `components/ui/Modal`
</verification>

<success_criteria>
- All three modals (CircleFormationModal, AddMemberModal, ConfirmDialog) use the shared Modal wrapper — focus trapping and Escape-close work via the wrapper
- ConsentBanner "Manage Preferences" button has aria-expanded
- ManageChallengeRow disclosure buttons have aria-expanded
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/21-interactive-component-remediation/21-01-SUMMARY.md`
</output>
