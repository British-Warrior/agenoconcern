---
phase: 11-kiosk-mode-and-institutional-pages
plan: 03
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - packages/web/src/pages/institution/InstitutionLanding.tsx
  - packages/web/src/App.tsx
  - packages/web/src/lib/constants.ts
autonomous: false

must_haves:
  truths:
    - "Each institution has a public page at /i/[slug] showing name, description, and local impact stats"
    - "Navigating from an institutional landing page auto-activates kiosk mode"
  artifacts:
    - path: "packages/web/src/pages/institution/InstitutionLanding.tsx"
      provides: "Public landing page fetching institution data and rendering name, description, stats, Get Started CTA"
      min_lines: 40
    - path: "packages/web/src/App.tsx"
      provides: "Route /i/:slug mapped to InstitutionLanding, outside ProtectedRoute"
      contains: "/i/:slug"
    - path: "packages/web/src/lib/constants.ts"
      provides: "INSTITUTION route constant"
      contains: "INSTITUTION"
  key_links:
    - from: "packages/web/src/pages/institution/InstitutionLanding.tsx"
      to: "/api/institutions/:slug"
      via: "fetch in useEffect or useQuery"
      pattern: "api/institutions"
    - from: "packages/web/src/pages/institution/InstitutionLanding.tsx"
      to: "packages/web/src/lib/constants.ts"
      via: "Get Started CTA links to ROUTES.LOGIN with ?kiosk=true"
      pattern: "kiosk=true"
    - from: "packages/web/src/App.tsx"
      to: "packages/web/src/pages/institution/InstitutionLanding.tsx"
      via: "Route element"
      pattern: "InstitutionLanding"
---

<objective>
Build the institutional landing page at /i/:slug and wire it into the app routing with kiosk auto-activation.

Purpose: Libraries and community centres need a public entry point showing their name, description, and local impact stats. Navigating from this page into the app auto-activates kiosk mode.
Output: InstitutionLanding page component, route wiring in App.tsx, ROUTES constant update.
</objective>

<execution_context>
@C:\Users\kirk_\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\kirk_\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-kiosk-mode-and-institutional-pages/11-RESEARCH.md
@.planning/phases/11-kiosk-mode-and-institutional-pages/11-01-SUMMARY.md
@.planning/phases/11-kiosk-mode-and-institutional-pages/11-02-SUMMARY.md
@packages/web/src/App.tsx
@packages/web/src/lib/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: InstitutionLanding page, route constant, and App.tsx routing</name>
  <files>
    packages/web/src/pages/institution/InstitutionLanding.tsx
    packages/web/src/lib/constants.ts
    packages/web/src/App.tsx
  </files>
  <action>
    1. Update `packages/web/src/lib/constants.ts`:
       - Add to ROUTES: `INSTITUTION: (slug: string) => \`/i/\${slug}\` as const`
       - Add: `INSTITUTION_BASE: "/i"` for route matching

    2. Create `packages/web/src/pages/institution/InstitutionLanding.tsx`:
       - Use `useParams<{ slug: string }>()` to get slug from URL
       - Fetch institution data from `GET /api/institutions/${slug}` using `useQuery` from TanStack Query (query key: `["institution", slug]`)
       - Handle loading state with a skeleton/spinner
       - Handle 404 with a "Institution not found" message
       - Render when loaded:
         a. Institution name as large heading (text-3xl md:text-4xl font-bold)
         b. City displayed below name if present (text-lg text-neutral-600)
         c. Description paragraph (text-lg, max-w-prose for readability)
         d. Impact stats in a 3-column grid of cards:
            - "Contributors" showing stats.contributors count
            - "Challenges" showing stats.challenges count
            - "Hours Contributed" showing stats.hours count
            - Each card: bg-white rounded-lg shadow p-6, large number (text-3xl font-bold text-primary-800), label below (text-sm text-neutral-600)
         e. "Get Started" CTA button — links to `${ROUTES.LOGIN}?kiosk=true`
            - Large button (min-h-[3.5rem] text-lg px-8) for kiosk touch screens
            - Uses `<Link>` to navigate, NOT `window.location`
       - The page is public (no auth required) — the route sits outside `<ProtectedRoute>` in App.tsx
       - This page does NOT import useKiosk — it doesn't need to know about kiosk context. It simply links to login with `?kiosk=true` in the URL, and `KioskProvider` (from Plan 01) will detect that param and activate kiosk mode (INST-03)

    3. Modify `packages/web/src/App.tsx`:
       - Import `InstitutionLanding` from `./pages/institution/InstitutionLanding.js`
       - Add route inside `<Route element={<AppShell />}>` among public routes:
         `<Route path="/i/:slug" element={<InstitutionLanding />} />`
       - Place it with the other public routes (near /login, /register, etc.)
  </action>
  <verify>
    `cd packages/web && npx tsc --noEmit` passes. `pnpm build` succeeds. Grep for `/i/:slug` in App.tsx confirms route exists. Grep for `kiosk=true` in InstitutionLanding.tsx confirms auto-activation link.
  </verify>
  <done>
    /i/:slug renders institution name, description, city, and 3 impact stat cards. "Get Started" CTA navigates to login with ?kiosk=true for auto-activation. Route is public (no auth required).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete kiosk mode and institutional landing pages:
    1. Kiosk mode activates via ?kiosk=true — simplified nav, larger buttons, no install/notification prompts
    2. End Session button visible in kiosk nav
    3. Idle timer: 10min inactivity triggers 60-second countdown overlay, then auto-logout
    4. Institution pages at /i/brixton-library and /i/manchester-central showing name, description, stats
    5. "Get Started" on institution page navigates to login with kiosk auto-activation
  </what-built>
  <how-to-verify>
    1. Start dev server: `pnpm dev`
    2. Visit http://localhost:5173/i/brixton-library — verify institution name, description, city, and 3 stat cards render
    3. Click "Get Started" — verify it navigates to /login?kiosk=true
    4. Visit http://localhost:5173/dashboard?kiosk=true (while logged in) — verify:
       a. "End Session" button visible in nav
       b. "Enable Notifications" and "Install App" buttons are hidden
       c. Nav links are larger than normal
       d. Footer is hidden
    5. Wait ~10 minutes idle (or temporarily reduce IDLE_TIMEOUT_MS to 15000ms for testing) — verify:
       a. 60-second countdown overlay appears
       b. "Continue Session" dismisses it and resets timer
       c. Letting countdown reach 0 logs out and redirects to /
    6. After kiosk logout: verify no private data remains (check Network tab — no stale queries)
    7. Visit http://localhost:5173/i/nonexistent — verify 404 message shown
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `cd packages/web && npx tsc --noEmit` — zero type errors
2. `pnpm build` — builds successfully
3. /i/:slug route exists in App.tsx outside ProtectedRoute
4. INSTITUTION route constant in constants.ts
5. InstitutionLanding fetches from /api/institutions/:slug
6. "Get Started" CTA includes ?kiosk=true for auto-activation
7. End-to-end: institution page -> Get Started -> login -> kiosk mode active
</verification>

<success_criteria>
- /i/brixton-library renders institution name, description, city, and impact stats
- /i/manchester-central renders similarly
- /i/nonexistent shows 404 message
- "Get Started" navigates to /login?kiosk=true
- After login from institution page, kiosk mode is active (End Session visible, install/notification hidden)
- ROUTES.INSTITUTION constant used (no hardcoded paths)
</success_criteria>

<output>
After completion, create `.planning/phases/11-kiosk-mode-and-institutional-pages/11-03-SUMMARY.md`
</output>
