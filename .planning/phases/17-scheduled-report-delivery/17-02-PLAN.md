---
phase: 17-scheduled-report-delivery
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - packages/web/src/pages/admin/InstitutionManagement.tsx
  - packages/web/src/api/admin.ts
  - packages/web/src/hooks/useInstitutions.ts
autonomous: false

must_haves:
  truths:
    - "Every delivery attempt (sent or failed) appears in a log with timestamp, status, and recipient email"
    - "CM can view delivery history for any institution in the admin UI"
    - "Failed deliveries show error message and retry status"
  artifacts:
    - path: "packages/web/src/pages/admin/InstitutionManagement.tsx"
      provides: "Delivery log viewer showing last 10 attempts"
      contains: "DeliveryLog"
  key_links:
    - from: "packages/web/src/pages/admin/InstitutionManagement.tsx"
      to: "/admin/institutions/:id/delivery-logs"
      via: "fetch in useDeliveryLogs hook"
      pattern: "delivery-logs"
---

<objective>
Add delivery log viewer UI in the institution detail panel and verify the full end-to-end delivery pipeline.

Purpose: SCHED-03 (delivery logging visible to CM) and verification of SCHED-04 (retry with backoff). Plan 17-01 creates the log table and retry logic; this plan surfaces it in the UI and verifies everything works together.
Output: CM can see delivery history; full pipeline verified via checkpoint.
</objective>

<execution_context>
@C:\Users\kirk_\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\kirk_\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-scheduled-report-delivery/17-RESEARCH.md
@.planning/phases/17-scheduled-report-delivery/17-01-SUMMARY.md

@packages/web/src/pages/admin/InstitutionManagement.tsx
@packages/web/src/api/admin.ts
@packages/web/src/hooks/useInstitutions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delivery log viewer in Institution detail panel</name>
  <files>
    packages/web/src/pages/admin/InstitutionManagement.tsx
    packages/web/src/hooks/useInstitutions.ts
  </files>
  <action>
1. In `packages/web/src/hooks/useInstitutions.ts`:
   - Add `useDeliveryLogs(institutionId: string | null)` hook — useQuery that calls `fetchDeliveryLogs(id)` (already created in 17-01's api/admin.ts). Enable only when institutionId is truthy. Return `{ data: DeliveryLog[], isLoading }`.

2. In `packages/web/src/pages/admin/InstitutionManagement.tsx`:
   - Below the "Report Delivery" toggle section (added in 17-01), add a "Delivery History" section.
   - Use `useDeliveryLogs(selectedInstitution?.id)` to fetch the last 10 log entries.
   - Render as a compact list/table with columns: Date (format attemptedAt as "DD Mon YYYY HH:mm"), Status (green "Sent" or red "Failed" badge), Recipient, and Error (show errorMessage on hover or inline if failed).
   - Show "No deliveries yet" when the list is empty.
   - Show attempt number for failed entries: "Attempt 3/5" format.
   - If a failed entry has nextRetryAt, show "Retry at [time]" in muted text.
   - Keep it minimal — no pagination needed (API already limits to 10).
   - Style with existing Tailwind patterns from the page (text-sm, rounded borders, etc.).
  </action>
  <verify>
    - `npx tsc --noEmit` passes in packages/web
    - The delivery log section renders in the institution detail panel
    - Empty state shows "No deliveries yet"
  </verify>
  <done>
    - CM can view the last 10 delivery attempts for any institution
    - Each entry shows timestamp, status (sent/failed), recipient email
    - Failed entries show error message and attempt count
    - Retry time shown for entries pending retry
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full scheduled report delivery pipeline:
    1. Contact email field on institution edit form
    2. Delivery toggle with weekly/monthly cadence selector
    3. Hourly cron job with advisory lock, PDF generation, Resend email
    4. Delivery log table with exponential backoff retry
    5. Log viewer in institution detail panel
  </what-built>
  <how-to-verify>
    1. Start the dev server (`npm run dev` in packages/server)
    2. Navigate to Institution Management in admin UI
    3. Select an institution and verify:
       a. Contact email field is visible and editable
       b. Report Delivery toggle is disabled when no contact email is set — helper text shown
       c. Set a contact email, then enable delivery with "Weekly" cadence
       d. Verify "Next delivery" date shows next Monday at 08:00 UTC
       e. Delivery History section shows "No deliveries yet"
    4. Check server console for "[report-delivery] Scheduled hourly" log on startup
    5. (Optional) To test actual delivery: temporarily set reportNextRunAt to a past date in the DB, wait for the next cron tick, check if email arrives and log entry appears
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Delivery log viewer renders with correct columns (date, status, recipient, error)
2. Empty state handled gracefully
3. Full TypeScript compilation passes across all packages
4. Server starts cleanly with both cron jobs registered
5. CM can complete full flow: set email -> enable schedule -> see next run date -> view delivery history
</verification>

<success_criteria>
- SCHED-03: Every delivery attempt visible in CM UI with timestamp, status, recipient
- SCHED-04: Failed entries show attempt count, error, and next retry time
- CM has full visibility into the automated delivery pipeline
</success_criteria>

<output>
After completion, create `.planning/phases/17-scheduled-report-delivery/17-02-SUMMARY.md`
</output>
