---
phase: 01-foundation-and-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - tsconfig.base.json
  - .gitignore
  - .env.example
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/index.ts
  - packages/shared/src/types/contributor.ts
  - packages/shared/src/types/auth.ts
  - packages/shared/src/schemas/auth.schemas.ts
  - packages/shared/src/schemas/contributor.schemas.ts
  - packages/shared/src/constants.ts
  - packages/server/package.json
  - packages/server/tsconfig.json
  - packages/server/drizzle.config.ts
  - packages/server/src/index.ts
  - packages/server/src/express-app.ts
  - packages/server/src/mcp-server.ts
  - packages/server/src/config/env.ts
  - packages/server/src/middleware/auth.ts
  - packages/server/src/middleware/error-handler.ts
  - packages/server/src/db/schema.ts
  - packages/server/src/db/index.ts
  - packages/server/src/tools/index.ts
  - packages/server/src/tools/contributors/get-contributor-profile.ts
  - packages/server/src/tools/contributors/update-contributor-profile.ts
  - packages/server/src/tools/contributors/get-contributor-circles.ts
  - packages/server/src/tools/contributors/get-contributor-impact.ts
  - packages/server/src/tools/challenges/list-challenges.ts
  - packages/server/src/tools/challenges/get-challenge-detail.ts
  - packages/server/src/tools/challenges/express-interest.ts
  - packages/server/src/tools/challenges/submit-challenge.ts
  - packages/server/src/tools/circles/get-circle-detail.ts
  - packages/server/src/tools/circles/add-circle-note.ts
  - packages/server/src/tools/circles/get-circle-notes.ts
  - packages/server/src/tools/circles/submit-resolution.ts
  - packages/server/src/tools/circles/update-social-link.ts
  - packages/server/src/tools/wellbeing/submit-wellbeing-checkin.ts
  - packages/web/package.json
  - packages/web/tsconfig.json
  - packages/web/vite.config.ts
  - packages/web/index.html
autonomous: true

must_haves:
  truths:
    - "pnpm install succeeds with all three workspace packages resolving"
    - "TypeScript compiles in all three packages with shared type imports working"
    - "Express server starts and responds to GET /health with 200"
    - "MCP server initialises with 14 tool stubs registered"
    - "Drizzle schema compiles and migration SQL can be generated"
    - "Vite dev server starts and renders a placeholder page"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Workspace configuration"
      contains: "packages/*"
    - path: "packages/shared/src/index.ts"
      provides: "Shared type re-exports"
      exports: ["Contributor", "AuthProvider", "Session"]
    - path: "packages/server/src/db/schema.ts"
      provides: "Drizzle schema for Phase 1 tables"
      contains: "contributors"
    - path: "packages/server/src/mcp-server.ts"
      provides: "MCP server with tool registration"
      contains: "McpServer"
    - path: "packages/server/src/express-app.ts"
      provides: "Express app with middleware"
      contains: "express"
    - path: "packages/server/src/tools/index.ts"
      provides: "All 14 MCP tool registrations"
      contains: "registerAllTools"
  key_links:
    - from: "packages/server/package.json"
      to: "@agenoconcern/shared"
      via: "workspace:* dependency"
      pattern: "workspace:\\*"
    - from: "packages/web/package.json"
      to: "@agenoconcern/shared"
      via: "workspace:* dependency"
      pattern: "workspace:\\*"
    - from: "packages/server/src/mcp-server.ts"
      to: "packages/server/src/tools/index.ts"
      via: "registerAllTools import"
      pattern: "registerAllTools"
---

<objective>
Scaffold the pnpm monorepo with three packages (shared, server, web), create the PostgreSQL database schema with Drizzle ORM, set up the Express server skeleton, register all 14 MCP tool stubs, and create the Vite web shell.

Purpose: Everything in Phase 1 (auth, UI, GDPR) depends on this foundation. Without it, nothing builds.
Output: Working monorepo where `pnpm install` succeeds, server starts, MCP tools are registered as stubs, and Vite dev server renders a placeholder.
</objective>

<execution_context>
@C:\Users\kirk_\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\kirk_\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-auth/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Monorepo scaffold with shared types and database schema</name>
  <files>
    package.json
    pnpm-workspace.yaml
    tsconfig.base.json
    .gitignore
    .env.example
    packages/shared/package.json
    packages/shared/tsconfig.json
    packages/shared/src/index.ts
    packages/shared/src/types/contributor.ts
    packages/shared/src/types/auth.ts
    packages/shared/src/schemas/auth.schemas.ts
    packages/shared/src/schemas/contributor.schemas.ts
    packages/shared/src/constants.ts
    packages/server/package.json
    packages/server/tsconfig.json
    packages/server/drizzle.config.ts
    packages/server/src/config/env.ts
    packages/server/src/db/schema.ts
    packages/server/src/db/index.ts
    packages/web/package.json
    packages/web/tsconfig.json
    packages/web/vite.config.ts
    packages/web/index.html
  </files>
  <action>
    1. Create root workspace:
       - `package.json` with `"private": true`, scripts for dev/build/db tasks
       - `pnpm-workspace.yaml` with `packages: ["packages/*"]`
       - `tsconfig.base.json` with target ES2022, module Node16, strict mode, paths for `@agenoconcern/shared`
       - `.gitignore` (node_modules, dist, .env, *.local, drizzle meta)
       - `.env.example` with all env vars from research (DATABASE_URL, JWT_SECRET, GOOGLE_CLIENT_ID/SECRET, LINKEDIN_CLIENT_ID/SECRET, TWILIO_ACCOUNT_SID/AUTH_TOKEN/VERIFY_SERVICE_SID, RESEND_API_KEY, PORT, NODE_ENV, CLIENT_URL)

    2. Create `@agenoconcern/shared` package:
       - `package.json` with `"main": "src/index.ts"`, `"types": "src/index.ts"` (no build step -- consumed as TS source)
       - `tsconfig.json` extending base
       - `src/types/contributor.ts`: Contributor type with id, name, email (nullable), username (nullable), phoneNumber (nullable), authProvider, role, status, createdAt, updatedAt, createdBy (nullable)
       - `src/types/auth.ts`: AuthProvider enum ("email" | "google" | "linkedin" | "phone" | "cm_created"), ContributorRole enum, ContributorStatus enum, TokenPayload type (sub, role, type?), OAuthProfile type
       - `src/schemas/auth.schemas.ts`: Zod schemas for registerInput (name, email, password), loginInput (email, password), phoneInput (phoneNumber in E.164), resetPasswordInput (token, newPassword)
       - `src/schemas/contributor.schemas.ts`: Zod schemas for contributor validation
       - `src/constants.ts`: Status enums, role constants, auth provider constants
       - `src/index.ts`: Re-exports all types, schemas, and constants

    3. Create `@agenoconcern/server` package skeleton:
       - `package.json` with `@agenoconcern/shared` as `workspace:*` dependency. Add all server deps: `@modelcontextprotocol/sdk`, `zod`, `express`, `cors`, `cookie-parser`, `drizzle-orm`, `postgres`, `arctic`, `argon2`, `jose`, `twilio`, `resend`. Dev deps: `@types/express`, `@types/cors`, `@types/cookie-parser`, `drizzle-kit`, `tsx`, `vitest`.
       - `tsconfig.json` extending base
       - `drizzle.config.ts` pointing to `src/db/schema.ts`, dialect "postgresql", dbCredentials from DATABASE_URL env
       - `src/config/env.ts`: Zod-validated env loading for all env vars (with sensible defaults for dev)

    4. Create Drizzle database schema (`packages/server/src/db/schema.ts`):
       - `authProvider` pgEnum: "email", "google", "linkedin", "phone", "cm_created"
       - `contributorRole` pgEnum: "contributor", "community_manager", "admin"
       - `contributorStatus` pgEnum: "onboarding", "active", "paused", "inactive"
       - `contributors` table: id (uuid PK), name (text, not null), email (varchar 255, unique, nullable), username (varchar 100, unique, nullable), phoneNumber (varchar 20, unique, nullable), passwordHash (text, nullable), authProvider (enum, not null), authProviderId (varchar 255, nullable), role (enum, default contributor), status (enum, default onboarding), createdAt, updatedAt, createdBy (self-reference, nullable)
       - `passwordResetTokens` table: id (uuid PK), contributorId (FK), tokenHash (text, not null), expiresAt (timestamp, not null), usedAt (timestamp, nullable), createdAt
       - `oauthAccounts` table: id (uuid PK), contributorId (FK), provider (varchar 20), providerAccountId (varchar 255), accessToken (text, nullable), refreshToken (text, nullable), tokenExpiresAt (timestamp, nullable), profileSnapshot (jsonb, nullable), createdAt, updatedAt
       - `consentRecords` table: id (uuid PK), contributorId (FK, nullable for pre-auth cookie consent), purpose (varchar 50), granted (boolean), policyVersion (varchar 20), ipAddress (varchar 45), userAgent (text), grantedAt, withdrawnAt
       - Export all tables and enums
       - `src/db/index.ts`: Database connection pool using `postgres` driver + drizzle wrapper. Reads DATABASE_URL from env.

    5. Create `@agenoconcern/web` package skeleton:
       - `package.json` with `@agenoconcern/shared` as `workspace:*` dependency. Add: `react`, `react-dom`, `react-router`, `@tanstack/react-query`, `react-cookie-consent`. Dev deps: `vite`, `@vitejs/plugin-react`, `tailwindcss`, `@tailwindcss/vite`, `@types/react`, `@types/react-dom`, `typescript`.
       - `tsconfig.json` extending base with jsx "react-jsx"
       - `vite.config.ts` with react plugin, tailwindcss plugin, server proxy from `/api` to `http://localhost:3000`
       - `index.html` with root div, Inter font from Google Fonts CDN, viewport meta

    6. Run `pnpm install` to resolve all workspace dependencies.

    IMPORTANT: Use pnpm (per research recommendation + user context). Shared package uses source TS directly -- NO build step.
  </action>
  <verify>
    Run `pnpm install` -- must succeed with 0 errors.
    Run `cd packages/server && npx tsc --noEmit` -- must compile.
    Run `cd packages/shared && npx tsc --noEmit` -- must compile.
    Run `cd packages/web && npx tsc --noEmit` -- must compile.
  </verify>
  <done>
    All three packages install and compile. Drizzle schema exports all Phase 1 tables. Shared types importable from both server and web. .env.example lists all required environment variables.
  </done>
</task>

<task type="auto">
  <name>Task 2: Express server, MCP tool stubs, and Vite web shell</name>
  <files>
    packages/server/src/index.ts
    packages/server/src/express-app.ts
    packages/server/src/mcp-server.ts
    packages/server/src/middleware/auth.ts
    packages/server/src/middleware/error-handler.ts
    packages/server/src/tools/index.ts
    packages/server/src/tools/contributors/get-contributor-profile.ts
    packages/server/src/tools/contributors/update-contributor-profile.ts
    packages/server/src/tools/contributors/get-contributor-circles.ts
    packages/server/src/tools/contributors/get-contributor-impact.ts
    packages/server/src/tools/challenges/list-challenges.ts
    packages/server/src/tools/challenges/get-challenge-detail.ts
    packages/server/src/tools/challenges/express-interest.ts
    packages/server/src/tools/challenges/submit-challenge.ts
    packages/server/src/tools/circles/get-circle-detail.ts
    packages/server/src/tools/circles/add-circle-note.ts
    packages/server/src/tools/circles/get-circle-notes.ts
    packages/server/src/tools/circles/submit-resolution.ts
    packages/server/src/tools/circles/update-social-link.ts
    packages/server/src/tools/wellbeing/submit-wellbeing-checkin.ts
    packages/web/src/main.tsx
    packages/web/src/App.tsx
  </files>
  <action>
    1. Create Express app (`packages/server/src/express-app.ts`):
       - Import express, cors, cookie-parser
       - Configure CORS with origin from CLIENT_URL env, credentials: true
       - Mount cookie-parser middleware
       - Mount JSON body parser
       - Add `GET /health` returning `{ status: "ok", timestamp: new Date().toISOString() }`
       - Mount error handler middleware (last)
       - Export the app (do NOT listen here)

    2. Create auth middleware stub (`packages/server/src/middleware/auth.ts`):
       - Export `authMiddleware` that reads access_token cookie, verifies JWT with jose, attaches `req.contributor` with { id, role }
       - Export `requireRole(role)` middleware that checks `req.contributor.role`
       - For now, JWT_SECRET comes from env config
       - This is a working middleware skeleton -- Plan 02 will use it as-is

    3. Create error handler (`packages/server/src/middleware/error-handler.ts`):
       - Express error handler (4 params). Log error. Return `{ error: message }` with appropriate status code. In production, sanitise error messages.

    4. Create all 14 MCP tool stubs following research Pattern 5:
       - Each tool in its own file under `packages/server/src/tools/{domain}/`
       - Each tool has FULL Zod input schema with `.describe()` on every field
       - Each tool returns `{ content: [{ type: "text", text: JSON.stringify({ error: "NOT_IMPLEMENTED", message: "...will be implemented in Phase N", tool: toolName, received: params }) }], isError: true }`
       - Contributors domain (4): get_contributor_profile (contributor_id: uuid), update_contributor_profile (contributor_id: uuid, updates: object), get_contributor_circles (contributor_id: uuid), get_contributor_impact (contributor_id: uuid)
       - Challenges domain (4): list_challenges (filters: optional object with domain, type, skills), get_challenge_detail (challenge_id: uuid), express_interest (challenge_id: uuid, contributor_id: uuid), submit_challenge (title, brief, domain, skills_needed, type, payment, deadline)
       - Circles domain (5): get_circle_detail (circle_id: uuid), add_circle_note (circle_id: uuid, contributor_id: uuid, content: string, type: enum), get_circle_notes (circle_id: uuid), submit_resolution (circle_id: uuid, content: object), update_social_link (circle_id: uuid, platform: enum, url: string)
       - Wellbeing domain (1): submit_wellbeing_checkin (contributor_id: uuid, ucla_score: number, wemwbs_score: number, freetext_note: optional string)

    5. Create MCP server (`packages/server/src/mcp-server.ts`):
       - Import McpServer from `@modelcontextprotocol/sdk/server/mcp.js`
       - Create server instance with name "agenoconcern", version "0.1.0"
       - Import and call `registerAllTools(server)` from tools/index.ts
       - Export the server instance

    6. Create `packages/server/src/tools/index.ts`:
       - Import all 14 register functions
       - Export `registerAllTools(server: McpServer)` that calls all of them

    7. Create server entry point (`packages/server/src/index.ts`):
       - Import express app from express-app.ts
       - Import MCP server
       - Start Express listening on PORT env (default 3000)
       - Log startup message with port
       - NOTE: MCP transport mounting (Streamable HTTP) can be added later. For now, MCP server is initialised and tools are registered but not exposed via HTTP transport yet. The server.tool() registrations are the important part.

    8. Create minimal Vite web app:
       - `packages/web/src/main.tsx`: ReactDOM.createRoot, render App
       - `packages/web/src/App.tsx`: Simple placeholder with "Age No Concern" heading and "Foundation loading..." text. Import from `@agenoconcern/shared` to verify workspace link works (e.g., import a constant).

    9. Add root package.json scripts:
       - `"dev:server": "pnpm --filter @agenoconcern/server dev"` (runs tsx watch on src/index.ts)
       - `"dev:web": "pnpm --filter @agenoconcern/web dev"` (runs vite dev)
       - `"dev": "pnpm run dev:server & pnpm run dev:web"` (both in parallel)
       - `"db:generate": "pnpm --filter @agenoconcern/server drizzle-kit generate"`
       - `"db:push": "pnpm --filter @agenoconcern/server drizzle-kit push"`
       - Server package.json scripts: `"dev": "tsx watch src/index.ts"`, `"build": "tsc"`, `"drizzle-kit": "drizzle-kit"`
       - Web package.json scripts: `"dev": "vite"`, `"build": "vite build"`
  </action>
  <verify>
    Run `cd packages/server && npx tsx src/index.ts` -- server starts on port 3000 without DB connection errors (DB not required for startup, connection is lazy).
    Run `curl http://localhost:3000/health` -- returns 200 with JSON body.
    Run `cd packages/web && npx vite --port 5173` -- Vite dev server starts.
    Verify MCP registration: In server startup, add a log line counting registered tools. Should log 14.
  </verify>
  <done>
    Express server starts and serves /health. All 14 MCP tools registered as stubs with full Zod schemas. Vite dev server starts and renders placeholder. Auth middleware skeleton ready for Plan 02. Root dev scripts work.
  </done>
</task>

</tasks>

<verification>
1. `pnpm install` succeeds from root
2. `pnpm run dev:server` starts Express on port 3000
3. `GET /health` returns 200
4. MCP server logs 14 registered tools
5. `pnpm run dev:web` starts Vite on port 5173
6. Browser loads placeholder page
7. TypeScript compiles in all three packages
8. Shared types import correctly from both server and web
</verification>

<success_criteria>
- Working pnpm monorepo with three packages that compile and resolve workspace dependencies
- Express server starts with health endpoint, CORS, cookie parsing, and error handling
- 14 MCP tool stubs registered with complete Zod input schemas
- Drizzle schema defines contributors, passwordResetTokens, oauthAccounts, and consentRecords tables
- Vite dev server renders placeholder and imports from shared package
- .env.example documents all required environment variables
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-auth/01-01-SUMMARY.md`
</output>
