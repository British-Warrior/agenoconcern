---
phase: 01-foundation-and-auth
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/server/src/index.ts
  - packages/server/src/express-app.ts
  - packages/server/src/mcp-server.ts
  - packages/server/src/middleware/auth.ts
  - packages/server/src/middleware/error-handler.ts
  - packages/server/src/tools/index.ts
  - packages/server/src/tools/contributors/get-contributor-profile.ts
  - packages/server/src/tools/contributors/update-contributor-profile.ts
  - packages/server/src/tools/contributors/get-contributor-circles.ts
  - packages/server/src/tools/contributors/get-contributor-impact.ts
  - packages/server/src/tools/challenges/list-challenges.ts
  - packages/server/src/tools/challenges/get-challenge-detail.ts
  - packages/server/src/tools/challenges/express-interest.ts
  - packages/server/src/tools/challenges/submit-challenge.ts
  - packages/server/src/tools/circles/get-circle-detail.ts
  - packages/server/src/tools/circles/add-circle-note.ts
  - packages/server/src/tools/circles/get-circle-notes.ts
  - packages/server/src/tools/circles/submit-resolution.ts
  - packages/server/src/tools/circles/update-social-link.ts
  - packages/server/src/tools/wellbeing/submit-wellbeing-checkin.ts
  - packages/web/src/main.tsx
  - packages/web/src/App.tsx
autonomous: true

must_haves:
  truths:
    - "Express server starts and responds to GET /health with 200"
    - "MCP server initialises with 14 tool stubs registered"
    - "Vite dev server starts and renders a placeholder page"
  artifacts:
    - path: "packages/server/src/express-app.ts"
      provides: "Express app with middleware"
      contains: "express"
    - path: "packages/server/src/mcp-server.ts"
      provides: "MCP server with tool registration"
      contains: "McpServer"
    - path: "packages/server/src/tools/index.ts"
      provides: "All 14 MCP tool registrations"
      contains: "registerAllTools"
    - path: "packages/server/src/middleware/auth.ts"
      provides: "JWT auth middleware skeleton"
      exports: ["authMiddleware", "requireRole"]
  key_links:
    - from: "packages/server/src/mcp-server.ts"
      to: "packages/server/src/tools/index.ts"
      via: "registerAllTools import"
      pattern: "registerAllTools"
    - from: "packages/server/src/index.ts"
      to: "packages/server/src/express-app.ts"
      via: "app import and listen"
      pattern: "app\\.listen"
---

<objective>
Set up the Express server with middleware, register all 14 MCP tool stubs, create the server entry point, and add a minimal Vite web placeholder.

Purpose: Provides the running server that Plan 01-03 (auth) mounts routes onto, and the MCP tool registry that later phases implement.
Output: Express server responding on /health, 14 MCP tools registered as stubs, Vite dev server rendering placeholder.
</objective>

<execution_context>
@C:\Users\kirk_\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\kirk_\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-auth/01-RESEARCH.md
@.planning/phases/01-foundation-and-auth/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Express server, middleware, and entry point</name>
  <files>
    packages/server/src/express-app.ts
    packages/server/src/middleware/auth.ts
    packages/server/src/middleware/error-handler.ts
    packages/server/src/index.ts
    packages/web/src/main.tsx
    packages/web/src/App.tsx
  </files>
  <action>
    1. Create Express app (`packages/server/src/express-app.ts`):
       - Import express, cors, cookie-parser
       - Configure CORS with origin from CLIENT_URL env, credentials: true
       - Mount cookie-parser middleware
       - Mount JSON body parser
       - Add `GET /health` returning `{ status: "ok", timestamp: new Date().toISOString() }`
       - Mount error handler middleware (last)
       - Export the app (do NOT listen here)

    2. Create auth middleware stub (`packages/server/src/middleware/auth.ts`):
       - Export `authMiddleware` that reads access_token cookie, verifies JWT with jose, attaches `req.contributor` with { id, role }
       - Export `requireRole(role)` middleware that checks `req.contributor.role`
       - For now, JWT_SECRET comes from env config
       - This is a working middleware skeleton -- Plan 01-03 will use it as-is

    3. Create error handler (`packages/server/src/middleware/error-handler.ts`):
       - Express error handler (4 params). Log error. Return `{ error: message }` with appropriate status code. In production, sanitise error messages.

    4. Create server entry point (`packages/server/src/index.ts`):
       - Import express app from express-app.ts
       - Import MCP server
       - Start Express listening on PORT env (default 3000)
       - Log startup message with port
       - NOTE: MCP transport mounting (Streamable HTTP) can be added later. For now, MCP server is initialised and tools are registered but not exposed via HTTP transport yet.

    5. Create minimal Vite web app:
       - `packages/web/src/main.tsx`: ReactDOM.createRoot, render App
       - `packages/web/src/App.tsx`: Simple placeholder with "Age No Concern" heading and "Foundation loading..." text. Import from `@agenoconcern/shared` to verify workspace link works (e.g., import a constant).

    6. Add root package.json scripts:
       - `"dev:server": "pnpm --filter @agenoconcern/server dev"` (runs tsx watch on src/index.ts)
       - `"dev:web": "pnpm --filter @agenoconcern/web dev"` (runs vite dev)
       - `"dev": "pnpm run dev:server & pnpm run dev:web"` (both in parallel)
       - `"db:generate": "pnpm --filter @agenoconcern/server drizzle-kit generate"`
       - `"db:push": "pnpm --filter @agenoconcern/server drizzle-kit push"`
       - Server package.json scripts: `"dev": "tsx watch src/index.ts"`, `"build": "tsc"`, `"drizzle-kit": "drizzle-kit"`
       - Web package.json scripts: `"dev": "vite"`, `"build": "vite build"`
  </action>
  <verify>
    Run `cd packages/server && npx tsx src/index.ts` -- server starts on port 3000 without DB connection errors (DB not required for startup, connection is lazy).
    Run `curl http://localhost:3000/health` -- returns 200 with JSON body.
    Run `cd packages/web && npx vite --port 5173` -- Vite dev server starts.
  </verify>
  <done>
    Express server starts and serves /health. Auth middleware skeleton ready for Plan 01-03. Vite dev server starts and renders placeholder. Root dev scripts work.
  </done>
</task>

<task type="auto">
  <name>Task 2: MCP server and 14 tool stubs</name>
  <files>
    packages/server/src/mcp-server.ts
    packages/server/src/tools/index.ts
    packages/server/src/tools/contributors/get-contributor-profile.ts
    packages/server/src/tools/contributors/update-contributor-profile.ts
    packages/server/src/tools/contributors/get-contributor-circles.ts
    packages/server/src/tools/contributors/get-contributor-impact.ts
    packages/server/src/tools/challenges/list-challenges.ts
    packages/server/src/tools/challenges/get-challenge-detail.ts
    packages/server/src/tools/challenges/express-interest.ts
    packages/server/src/tools/challenges/submit-challenge.ts
    packages/server/src/tools/circles/get-circle-detail.ts
    packages/server/src/tools/circles/add-circle-note.ts
    packages/server/src/tools/circles/get-circle-notes.ts
    packages/server/src/tools/circles/submit-resolution.ts
    packages/server/src/tools/circles/update-social-link.ts
    packages/server/src/tools/wellbeing/submit-wellbeing-checkin.ts
  </files>
  <action>
    1. Create all 14 MCP tool stubs following research Pattern 5:
       - Each tool in its own file under `packages/server/src/tools/{domain}/`
       - Each tool has FULL Zod input schema with `.describe()` on every field
       - Each tool returns `{ content: [{ type: "text", text: JSON.stringify({ error: "NOT_IMPLEMENTED", message: "...will be implemented in Phase N", tool: toolName, received: params }) }], isError: true }`
       - Contributors domain (4): get_contributor_profile (contributor_id: uuid), update_contributor_profile (contributor_id: uuid, updates: object), get_contributor_circles (contributor_id: uuid), get_contributor_impact (contributor_id: uuid)
       - Challenges domain (4): list_challenges (filters: optional object with domain, type, skills), get_challenge_detail (challenge_id: uuid), express_interest (challenge_id: uuid, contributor_id: uuid), submit_challenge (title, brief, domain, skills_needed, type, payment, deadline)
       - Circles domain (5): get_circle_detail (circle_id: uuid), add_circle_note (circle_id: uuid, contributor_id: uuid, content: string, type: enum), get_circle_notes (circle_id: uuid), submit_resolution (circle_id: uuid, content: object), update_social_link (circle_id: uuid, platform: enum, url: string)
       - Wellbeing domain (1): submit_wellbeing_checkin (contributor_id: uuid, ucla_score: number, wemwbs_score: number, freetext_note: optional string)

    2. Create `packages/server/src/tools/index.ts`:
       - Import all 14 register functions
       - Export `registerAllTools(server: McpServer)` that calls all of them

    3. Create MCP server (`packages/server/src/mcp-server.ts`):
       - Import McpServer from `@modelcontextprotocol/sdk/server/mcp.js`
       - Create server instance with name "agenoconcern", version "0.1.0"
       - Import and call `registerAllTools(server)` from tools/index.ts
       - Export the server instance
  </action>
  <verify>
    Verify MCP registration: In server startup, add a log line counting registered tools. Should log 14.
    TypeScript compiles: `cd packages/server && npx tsc --noEmit`
  </verify>
  <done>
    All 14 MCP tools registered as stubs with full Zod schemas. MCP server initialised with all tools. Tool stubs return NOT_IMPLEMENTED responses with received params for debugging.
  </done>
</task>

</tasks>

<verification>
1. `pnpm run dev:server` starts Express on port 3000
2. `GET /health` returns 200
3. MCP server logs 14 registered tools
4. `pnpm run dev:web` starts Vite on port 5173
5. Browser loads placeholder page
6. TypeScript compiles in all packages
</verification>

<success_criteria>
- Express server starts with health endpoint, CORS, cookie parsing, and error handling
- 14 MCP tool stubs registered with complete Zod input schemas
- Auth middleware skeleton ready for auth plan
- Vite dev server renders placeholder and imports from shared package
- Root dev scripts work for both server and web
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-auth/01-02-SUMMARY.md`
</output>
