---
phase: 15-pdf-impact-report
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - packages/web/src/api/admin.ts
  - packages/web/src/pages/admin/InstitutionManagement.tsx
autonomous: false

must_haves:
  truths:
    - "CM sees a 'Generate Report' button on each institution card"
    - "Clicking the button downloads a PDF file to the browser"
    - "Button shows loading state while PDF is generating"
    - "Button is disabled when institution has no contributors (stats is null)"
    - "CM can optionally select a date range before generating"
    - "422 error from server displays a user-friendly message (not a broken download)"
  artifacts:
    - path: "packages/web/src/api/admin.ts"
      provides: "downloadInstitutionReport function using raw fetch + blob"
      contains: "downloadInstitutionReport"
    - path: "packages/web/src/pages/admin/InstitutionManagement.tsx"
      provides: "Generate Report button with loading state and date range inputs"
      contains: "Generate Report"
  key_links:
    - from: "packages/web/src/pages/admin/InstitutionManagement.tsx"
      to: "packages/web/src/api/admin.ts"
      via: "import downloadInstitutionReport"
      pattern: "downloadInstitutionReport"
    - from: "packages/web/src/api/admin.ts"
      to: "/api/admin/institutions/:slug/report.pdf"
      via: "raw fetch with credentials: include"
      pattern: "report\\.pdf"
---

<objective>
Add the "Generate Report" button with date range inputs to the institution management page, and the raw-fetch download function.

Purpose: Completes the CM-facing UI for PDF-01 (on-demand trigger) with loading/error states and optional date filtering.
Output: CM can click a button to download a branded PDF for any institution.
</objective>

<execution_context>
@C:\Users\kirk_\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\kirk_\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-pdf-impact-report/15-RESEARCH.md
@.planning/phases/15-pdf-impact-report/15-01-SUMMARY.md
@packages/web/src/pages/admin/InstitutionManagement.tsx
@packages/web/src/api/admin.ts
@packages/web/src/api/client.ts
@packages/web/src/lib/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add downloadInstitutionReport function and Generate Report UI</name>
  <files>
    packages/web/src/api/admin.ts
    packages/web/src/pages/admin/InstitutionManagement.tsx
  </files>
  <action>
**A. Add download function to `packages/web/src/api/admin.ts`:**

Add `downloadInstitutionReport` at the bottom of the file. This function must NOT use `apiClient` (it only handles JSON). Use raw `fetch` instead:

```typescript
export async function downloadInstitutionReport(
  slug: string,
  from?: string,
  to?: string,
): Promise<void> {
  const params = new URLSearchParams();
  if (from) params.set("from", from);
  if (to) params.set("to", to);
  const query = params.toString() ? `?${params.toString()}` : "";

  const res = await fetch(
    `${API_BASE_URL}/api/admin/institutions/${slug}/report.pdf${query}`,
    { credentials: "include" },
  );

  if (!res.ok) {
    const err = await res.json().catch(() => ({ error: "Report generation failed" }));
    throw new Error((err as { error: string }).error);
  }

  const blob = await res.blob();
  const url = URL.createObjectURL(blob);
  const a = document.createElement("a");
  a.href = url;
  a.download = `impact-report-${slug}.pdf`;
  document.body.appendChild(a);
  a.click();
  document.body.removeChild(a);
  URL.revokeObjectURL(url);
}
```

Import `API_BASE_URL` from `../lib/constants.js` at the top of admin.ts (add if not already imported).

**B. Add Generate Report button to `InstitutionManagement.tsx`:**

Modify the `InstitutionCardView` component to add a report generation section. The button goes in the existing bottom action bar (the `flex items-center justify-between` div with the toggle and Edit button).

1. Add state to InstitutionCardView:
   - `isGenerating: boolean` (loading state)
   - `reportError: string | null` (error display)
   - `showDateRange: boolean` (toggle date inputs)
   - `fromDate: string` (ISO date string, initially empty)
   - `toDate: string` (ISO date string, initially empty)

2. Add a `handleGenerateReport` async function:
   - Sets isGenerating true, clears error
   - Calls `downloadInstitutionReport(institution.slug, fromDate || undefined, toDate || undefined)`
   - On catch: set reportError to error message
   - Finally: set isGenerating false

3. Add UI between the stats line and the expandable contributor list:
   - A collapsible date range section:
     - Small text link: "Date range: All time" (or the selected range). Clicking toggles showDateRange.
     - When expanded: two `<input type="date">` fields (From / To) side by side, with labels. Both optional.
   - "Generate Report" button:
     - Use `<Button variant="outline" size="default">` (matches existing codebase pattern)
     - `loading={isGenerating}` prop for loading spinner
     - `disabled={isGenerating || !institution.stats}` — disabled when no contributors (stats is null) or already generating
     - Text: "Generate Report" (or "Generating..." when loading, if Button component doesn't handle loading text)
   - Error display: if reportError is set, show a small red text below the button

4. Import `downloadInstitutionReport` from `../../api/admin.js` at the top.

Style notes:
- Date inputs: `className="border border-neutral-200 rounded-lg px-3 py-1.5 text-sm"` (matches Input component style)
- Error text: `className="text-xs text-red-600 mt-1"`
- Keep the button compact — it sits alongside Edit and the toggle in the card footer area. Add it between the toggle group and the Edit button.
  </action>
  <verify>
1. `cd packages/web && npx tsc --noEmit` compiles without errors.
2. Start dev server (`pnpm dev`), navigate to the institution management page.
3. Verify "Generate Report" button appears on each institution card.
4. Verify button is disabled for institutions with no contributors.
5. Click "Generate Report" on an institution with contributors — PDF should download.
6. Test with date range: expand date range, set from/to dates, generate — PDF should reflect filtered data.
  </verify>
  <done>
    - downloadInstitutionReport function exists in admin.ts using raw fetch + blob
    - InstitutionCardView shows "Generate Report" button
    - Button is disabled when stats is null (no contributors)
    - Button shows loading state during generation
    - Optional date range inputs are available (collapsed by default)
    - Errors from 422 (empty institution) display as user-friendly message
    - TypeScript compiles cleanly
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete PDF impact report feature — server-side PDF generation with branded styling, streamed to browser via CM UI button with date range support.</what-built>
  <how-to-verify>
    1. Navigate to the institution management page (CM or admin login)
    2. Find an institution WITH contributors assigned
    3. Click "Generate Report" — a PDF should download within a few seconds
    4. Open the PDF and verify it contains:
       - Navy header band with "Indomitable Unity" in white
       - Amber tagline text
       - "Impact Report" title with institution name
       - Date range label ("All time" if no dates selected)
       - Stats table: contributor count, challenge count, total hours
       - Footer with generation date
    5. Test date range: expand the date picker, set a from/to range, generate again — verify the stats reflect filtered data (or show zeros if no data in range)
    6. Test empty institution: find an institution with NO contributors — verify the button is disabled
    7. Verify a second download generates a fresh PDF (no caching — check the generation timestamp changes)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues with the PDF content, styling, or download behaviour</resume-signal>
</task>

</tasks>

<verification>
1. TypeScript: both packages compile without errors
2. PDF downloads successfully from institution management page
3. PDF content matches requirements (institution name, date range, contributor count, challenge count, hours)
4. PDF styling uses Indomitable Unity branding (navy, amber, Inter font)
5. No file stored in S3 — streaming only (verify no S3 calls in code)
6. Empty institution shows 422 error gracefully in UI
7. Date range filtering works correctly
</verification>

<success_criteria>
- CM can trigger PDF download from institution management page
- Downloaded PDF contains all required data fields
- PDF is streamed (no S3 storage)
- Brand styling applied (Inter font, navy/amber colours)
- Loading and error states work correctly
- Date range input is available and functional
</success_criteria>

<output>
After completion, create `.planning/phases/15-pdf-impact-report/15-02-SUMMARY.md`
</output>
