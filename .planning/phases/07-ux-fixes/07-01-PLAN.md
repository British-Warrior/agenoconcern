---
phase: 07-ux-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/lib/constants.ts
  - packages/web/src/components/layout/Navbar.tsx
  - packages/web/src/components/ui/Card.tsx
  - packages/web/src/components/circles/ResolutionCard.tsx
  - packages/web/src/components/challenges/TeamCompositionCard.tsx
  - packages/web/src/pages/circles/MyCircles.tsx
autonomous: true

must_haves:
  truths:
    - "User can reach dashboard, challenges, circles, impact, wellbeing, and logout from every authenticated page"
    - "All interactive cards, buttons, and links respond visually to hover and show pointer cursor"
  artifacts:
    - path: "packages/web/src/lib/constants.ts"
      provides: "ROUTES constants for CHALLENGES, CIRCLES, IMPACT, WELLBEING_CHECKIN"
      contains: "CHALLENGES|CIRCLES|IMPACT|WELLBEING_CHECKIN"
    - path: "packages/web/src/components/layout/Navbar.tsx"
      provides: "Full nav links for Dashboard, Challenges, My Circles, My Impact, Wellbeing, Logout"
      min_lines: 80
  key_links:
    - from: "packages/web/src/components/layout/Navbar.tsx"
      to: "packages/web/src/lib/constants.ts"
      via: "ROUTES import"
      pattern: "ROUTES\\.(DASHBOARD|CHALLENGES|CIRCLES|IMPACT|WELLBEING_CHECKIN)"
---

<objective>
Add missing navigation links and hover affordances across the platform.

Purpose: Users are currently stranded without links to Dashboard, Challenges, or Wellbeing from the navbar. Interactive elements lack hover feedback, making the UI feel static and unresponsive. These are the most visible UX gaps for institutional demos.

Output: Complete navbar with all authenticated links using ROUTES constants; hover states and cursor-pointer on all interactive cards and buttons across the app.
</objective>

<execution_context>
@C:\Users\kirk_\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\kirk_\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-ux-fixes/07-RESEARCH.md

@packages/web/src/lib/constants.ts
@packages/web/src/components/layout/Navbar.tsx
@packages/web/src/components/ui/Card.tsx
@packages/web/src/components/circles/ResolutionCard.tsx
@packages/web/src/components/challenges/TeamCompositionCard.tsx
@packages/web/src/pages/circles/MyCircles.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing ROUTES constants and complete Navbar links</name>
  <files>
    packages/web/src/lib/constants.ts
    packages/web/src/components/layout/Navbar.tsx
  </files>
  <action>
1. In `packages/web/src/lib/constants.ts`, add the missing route constants to the ROUTES object:
   - `CHALLENGES: "/challenges"`
   - `CIRCLES: "/circles"`
   - `IMPACT: "/impact"`
   - `WELLBEING_CHECKIN: "/wellbeing/checkin"`

2. In `packages/web/src/components/layout/Navbar.tsx`, add missing navigation links inside the `isAuthenticated` block. The links must appear in this order inside the `<div className="flex items-center gap-4">` after the `isAuthenticated` check:
   - Dashboard (to `ROUTES.DASHBOARD`)
   - Challenges (to `ROUTES.CHALLENGES`)
   - My Circles (update existing hardcoded `/circles` to `ROUTES.CIRCLES`)
   - My Impact (update existing hardcoded `/impact` to `ROUTES.IMPACT`)
   - Wellbeing (to `ROUTES.WELLBEING_CHECKIN`)
   - NotificationBell (already exists)
   - Enable Notifications button (already exists)
   - Install App button (already exists)
   - Contributor name (already exists)
   - Logout button (already exists)

   Each new link must use the exact className pattern already in use:
   `"text-sm font-medium text-neutral-700 hover:text-primary-800 transition-colors no-underline"`

   Import ROUTES is already present -- just reference the new constants.

   Additionally, add a `hidden sm:flex` responsive wrapper around the nav links (Dashboard through Wellbeing) so on mobile they collapse. Or, simpler: add `hidden sm:inline` to each link. The existing links already lack responsive hiding, so match their current behaviour (keep them all visible) -- do NOT add responsive hiding unless the existing links already have it. Since existing My Circles and My Impact links have no responsive classes, keep the new links the same way.
  </action>
  <verify>
    Run `cd /c/Users/kirk_/agenoconcern && npx tsc --noEmit -p packages/web/tsconfig.json` -- no type errors.
    Grep constants.ts for CHALLENGES, CIRCLES, IMPACT, WELLBEING_CHECKIN -- all present.
    Grep Navbar.tsx for ROUTES.DASHBOARD, ROUTES.CHALLENGES, ROUTES.CIRCLES, ROUTES.IMPACT, ROUTES.WELLBEING_CHECKIN -- all present. No hardcoded `/circles` or `/impact` strings remain.
  </verify>
  <done>
    Navbar renders Dashboard, Challenges, My Circles, My Impact, Wellbeing links for authenticated users. All link paths use ROUTES constants. UX-01 and UX-08 satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add hover states and cursor-pointer to interactive elements</name>
  <files>
    packages/web/src/components/ui/Card.tsx
    packages/web/src/components/circles/ResolutionCard.tsx
    packages/web/src/components/challenges/TeamCompositionCard.tsx
    packages/web/src/pages/circles/MyCircles.tsx
  </files>
  <action>
Audit and fix interactive elements that are missing `cursor-pointer` and/or hover visual feedback. The existing codebase already has `cursor-pointer` on Button.tsx and ChallengeRow.tsx. Fix the gaps:

1. **Card.tsx** -- The Card component itself is not interactive (it's a container), so do NOT add cursor-pointer to it. However, elements that wrap Cards in clickable contexts need fixing. Skip Card.tsx.

2. **MyCircles.tsx** -- The circle cards (the `<Link>` elements in the grid) already have `hover:border-primary-300 hover:shadow-sm` and `group` class. Links naturally have pointer cursor. These are fine. Skip.

3. **TeamCompositionCard.tsx** -- The "Select this team" button and "Form Circle" button already have hover states. But the overall card div does NOT have `cursor-pointer` when it's not a button. The card itself is not clickable (buttons inside are), so skip. However, ensure both buttons have `cursor-pointer` class explicitly (buttons have it by default in most browsers, but Tailwind's preflight may reset it). Add `cursor-pointer` to:
   - The "Select this team" / "Selected" toggle button (line ~122-132)
   - The "Form Circle" button (line ~137-142)

4. **ResolutionCard.tsx** -- The "Edit Resolution" button (line 133-139) is a `<button>` element but uses a custom className without `cursor-pointer`. Add `cursor-pointer` to its className.

5. **General sweep:** Check all `<button>` elements in `CircleWorkspaceShell.tsx` -- the "Add Member" button (line 226-229) already has hover state. The Collapsible.Trigger button already has hover. These use custom classNames without explicit `cursor-pointer`. Add `cursor-pointer` to:
   - ResolutionCard.tsx: "Edit Resolution" button
   - ResolutionCard.tsx: "Cancel" button in edit mode (line 102-108)
   - TeamCompositionCard.tsx: both action buttons

For each button, simply add `cursor-pointer` to the existing className string. Do NOT change any other styles.

Note: The `<Button>` component from `ui/Button.tsx` already includes `cursor-pointer` in its base styles, so any element using `<Button>` is already covered. Only raw `<button>` elements with custom classNames need the fix.
  </action>
  <verify>
    Grep for `<button` in the modified files and verify each has either `cursor-pointer` in its className or uses the `Button` component (which has it built-in).
    Run `cd /c/Users/kirk_/agenoconcern && npx tsc --noEmit -p packages/web/tsconfig.json` -- no type errors.
  </verify>
  <done>
    All interactive buttons in ResolutionCard and TeamCompositionCard have explicit cursor-pointer and hover visual feedback. UX-05 satisfied.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `npx tsc --noEmit -p packages/web/tsconfig.json`
2. Navbar has links for Dashboard, Challenges, My Circles, My Impact, Wellbeing (5 nav items + notifications + install + name + logout)
3. No hardcoded route strings in Navbar.tsx (all use ROUTES.*)
4. All `<button>` elements in modified files have cursor-pointer
</verification>

<success_criteria>
- ROUTES object has CHALLENGES, CIRCLES, IMPACT, WELLBEING_CHECKIN entries
- Navbar renders 5 navigation links for authenticated users
- All raw button elements in ResolutionCard and TeamCompositionCard have cursor-pointer
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-ux-fixes/07-01-SUMMARY.md`
</output>
