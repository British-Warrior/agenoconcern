---
phase: 04-circles-and-collaboration
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - packages/web/src/api/circles.ts
  - packages/web/src/hooks/useCircles.ts
  - packages/web/src/components/circles/CircleFormationModal.tsx
  - packages/web/src/components/circles/CircleWorkspaceShell.tsx
  - packages/web/src/components/circles/NoteComposer.tsx
  - packages/web/src/components/circles/NoteCard.tsx
  - packages/web/src/pages/circles/CircleWorkspace.tsx
  - packages/web/src/pages/circles/MyCircles.tsx
  - packages/web/src/components/challenges/TeamCompositionCard.tsx
  - packages/web/src/App.tsx
autonomous: true

must_haves:
  truths:
    - "CM can form a Circle from a selected team composition and land in the new workspace"
    - "Circle workspace shows pinned challenge brief, member list, and notes feed"
    - "Circle members can post notes with text and optional file attachments"
    - "Contributors can see a list of their active Circles and navigate to each workspace"
  artifacts:
    - path: "packages/web/src/api/circles.ts"
      provides: "API client functions for all circle endpoints"
      min_lines: 40
    - path: "packages/web/src/hooks/useCircles.ts"
      provides: "TanStack Query hooks for circles data"
      min_lines: 30
    - path: "packages/web/src/components/circles/CircleFormationModal.tsx"
      provides: "Modal for CM to confirm and create circle from selected team"
      min_lines: 30
    - path: "packages/web/src/pages/circles/CircleWorkspace.tsx"
      provides: "Main workspace page at /circles/:id"
      min_lines: 40
    - path: "packages/web/src/components/circles/NoteComposer.tsx"
      provides: "Note posting form with file attachment via react-dropzone"
      min_lines: 30
  key_links:
    - from: "packages/web/src/api/circles.ts"
      to: "/api/circles"
      via: "fetch wrapper calls"
      pattern: "api/circles"
    - from: "packages/web/src/components/circles/CircleFormationModal.tsx"
      to: "packages/web/src/api/circles.ts"
      via: "createCircle mutation"
      pattern: "createCircle|useCreateCircle"
    - from: "packages/web/src/components/circles/NoteComposer.tsx"
      to: "packages/web/src/api/circles.ts"
      via: "postNote mutation + presigned upload"
      pattern: "postNote|attachmentUrl"
    - from: "packages/web/src/App.tsx"
      to: "packages/web/src/pages/circles/CircleWorkspace.tsx"
      via: "React Router route /circles/:id"
      pattern: "circles/:id"
---

<objective>
Build the Circle formation flow and workspace UI: formation modal wiring Phase 3's "Select this team", workspace page with pinned brief and member list, notes feed with file attachment upload/download, My Circles list page, and all supporting API client functions and TanStack Query hooks.

Purpose: Delivers the core collaboration experience — contributors can be formed into Circles and immediately start posting notes with attachments in a shared workspace.
Output: Working Circle formation from CM admin, CircleWorkspace page at /circles/:id, My Circles list, notes feed with file attachments.
</objective>

<execution_context>
@C:\Users\kirk_\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\kirk_\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-circles-and-collaboration/04-RESEARCH.md
@.planning/phases/04-circles-and-collaboration/04-01-SUMMARY.md
@packages/web/src/api/challenges.ts
@packages/web/src/hooks/useChallenges.ts
@packages/web/src/components/challenges/TeamCompositionCard.tsx
@packages/web/src/App.tsx
@packages/web/src/styles/app.css
@packages/web/src/components/ui/Button.tsx
@packages/web/src/components/ui/Card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: API client, hooks, Circle formation modal, and TeamCompositionCard wiring</name>
  <files>
    packages/web/src/api/circles.ts
    packages/web/src/hooks/useCircles.ts
    packages/web/src/components/circles/CircleFormationModal.tsx
    packages/web/src/components/challenges/TeamCompositionCard.tsx
  </files>
  <action>
    1. Create packages/web/src/api/circles.ts — API client functions using the existing fetch wrapper pattern from challenges.ts:
       - createCircle(data: CreateCircleInput) — POST /api/circles
       - getMyCircles() — GET /api/circles
       - getCircleWorkspace(circleId: string) — GET /api/circles/:id
       - addMember(circleId: string, contributorId: string) — POST /api/circles/:id/members
       - getNotes(circleId: string, cursor?: string) — GET /api/circles/:id/notes?cursor=...
       - postNote(circleId: string, data: PostNoteInput) — POST /api/circles/:id/notes
       - getAttachmentUrl(circleId: string, data: { fileName, mimeType, fileSizeBytes }) — POST /api/circles/:id/notes/attachment-url
       - getDownloadUrl(circleId: string, noteId: string, attachmentId: string) — GET /api/circles/:id/notes/:noteId/download/:attachmentId
       - setSocialChannel(circleId: string, data: SetSocialChannelInput) — PUT /api/circles/:id/social
       - submitResolution(circleId: string, data: SubmitResolutionInput) — POST /api/circles/:id/resolution
       - updateResolution(circleId: string, data: SubmitResolutionInput) — PUT /api/circles/:id/resolution
       - getResolution(circleId: string) — GET /api/circles/:id/resolution
       - rateResolution(circleId: string, data: RateResolutionInput) — POST /api/circles/:id/resolution/rating

    2. Create packages/web/src/hooks/useCircles.ts — TanStack Query hooks following useChallenges.ts pattern:
       - useMyCircles() — useQuery(["circles"], getMyCircles)
       - useCircleWorkspace(circleId: string | null) — useQuery(["circles", circleId], ..., enabled: !!circleId)
       - useCircleNotes(circleId: string) — useInfiniteQuery(["circles", circleId, "notes"], cursor-based)
       - useCreateCircle() — useMutation, invalidates ["circles"] on success
       - usePostNote(circleId: string) — useMutation, invalidates ["circles", circleId, "notes"]
       - useSetSocialChannel(circleId: string) — useMutation, invalidates ["circles", circleId]
       - useSubmitResolution(circleId: string) — useMutation, invalidates ["circles", circleId, "resolution"]
       - useUpdateResolution(circleId: string) — useMutation, invalidates ["circles", circleId, "resolution"]
       - useResolution(circleId: string) — useQuery(["circles", circleId, "resolution"])
       - useRateResolution(circleId: string) — useMutation, invalidates ["circles", circleId, "resolution"]
       - useAddMember(circleId: string) — useMutation, invalidates ["circles", circleId]

    3. Create packages/web/src/components/circles/CircleFormationModal.tsx:
       - Props: isOpen, onClose, challengeId: string, memberIds: string[], memberNames: string[] (display only)
       - Shows modal overlay with list of selected members by name
       - "Form Circle" button calls useCreateCircle mutation with { challengeId, memberIds }
       - On success: navigate to /circles/{newCircle.id} using react-router useNavigate
       - On error: show error message (e.g., member at max circles)
       - "Cancel" button closes modal
       - Style with existing Card, Button components and Tailwind. Modal overlay with backdrop blur.

    4. Update packages/web/src/components/challenges/TeamCompositionCard.tsx:
       - Import CircleFormationModal
       - Add state: showFormationModal (boolean)
       - When team is selected (existing "Select this team" local state), show a new "Form Circle" button
       - "Form Circle" button sets showFormationModal = true
       - Render CircleFormationModal with isOpen={showFormationModal}, passing challengeId and the selected team's memberIds and member names
       - On modal close: reset showFormationModal
  </action>
  <verify>
    cd packages/web && npx tsc --noEmit — no TypeScript errors.
    Visually: The TeamCompositionCard should show "Form Circle" button after selecting a team.
  </verify>
  <done>
    API client covers all circle endpoints. TanStack Query hooks available for all circle data. CircleFormationModal renders and calls createCircle. TeamCompositionCard has "Form Circle" button that opens the modal.
  </done>
</task>

<task type="auto">
  <name>Task 2: CircleWorkspace page, notes feed, My Circles list, and routing</name>
  <files>
    packages/web/src/pages/circles/CircleWorkspace.tsx
    packages/web/src/pages/circles/MyCircles.tsx
    packages/web/src/components/circles/CircleWorkspaceShell.tsx
    packages/web/src/components/circles/NoteComposer.tsx
    packages/web/src/components/circles/NoteCard.tsx
    packages/web/src/App.tsx
  </files>
  <action>
    1. Create packages/web/src/pages/circles/MyCircles.tsx:
       - Uses useMyCircles() hook to fetch contributor's circles
       - Renders a list of CircleListItem cards: challenge title, circle status badge, member count, created date
       - Each card links to /circles/:id
       - Empty state: "You're not in any Circles yet. Express interest in challenges to get started."
       - Style with existing Card component, Tailwind grid (responsive: 1 col mobile, 2 cols md, 3 cols lg)

    2. Create packages/web/src/components/circles/NoteCard.tsx:
       - Props: note (CircleNote with attachments and author name)
       - Renders: author name, timestamp (relative — "2 hours ago" using simple date math, no library), note body text
       - For each attachment: show fileName + file size, download button that calls getDownloadUrl and opens the presigned URL in new tab
       - Use useCallback for download handler to avoid re-renders
       - Style: Card-like container, subtle border, compact layout

    3. Create packages/web/src/components/circles/NoteComposer.tsx:
       - Text input: textarea with placeholder "Share a note with your Circle..."
       - File attachment: react-dropzone zone below textarea, accepts PDF/DOCX/TXT/JPEG/PNG/WEBP, max 10MB, max 5 files
       - Upload flow: On submit, for each file: call getAttachmentUrl to get presigned URL + s3Key, PUT file to presigned URL, collect { s3Key, fileName, mimeType, fileSizeBytes }
       - After all uploads complete: call postNote mutation with { body, attachments }
       - Show upload progress per file (simple: "Uploading 2/3...")
       - Disable submit button while uploading or while body is empty
       - On success: clear form, focus textarea
       - Style: sticky bottom of notes feed area, Card container, Button for submit

    4. Create packages/web/src/components/circles/CircleWorkspaceShell.tsx:
       - Props: circle workspace data (from useCircleWorkspace)
       - Layout sections (top to bottom):
         a. Pinned challenge brief: Card with challenge title, brief text, domain badges, skills needed, type badge. Collapsible (Radix Collapsible, already installed) — expanded by default, user can collapse.
         b. Member list: horizontal row of member names with count badge. If CM, show "Add Member" button.
         c. Social channel link area: if socialChannelUrl exists, show platform icon + "Open in {channel}" link (opens window.open with noopener,noreferrer). If no link set, show "Set social channel" placeholder. (Full editor is Plan 03)
         d. Notes feed: renders list of NoteCard components from useCircleNotes infinite query, with intersection observer for infinite scroll (reuse pattern from challenge feed). Newest notes at top.
         e. NoteComposer at the bottom (sticky)
       - Status badge in header showing circle status (forming/active/submitted/completed/dissolved)

    5. Create packages/web/src/pages/circles/CircleWorkspace.tsx:
       - Route component for /circles/:id
       - Reads circleId from useParams
       - Uses useCircleWorkspace(circleId) for circle data
       - Loading state, error state (403 = "You're not a member of this Circle")
       - Renders CircleWorkspaceShell with workspace data

    6. Update packages/web/src/App.tsx:
       - Add routes (inside ProtectedRoute):
         - /circles → MyCircles page
         - /circles/:id → CircleWorkspace page
       - Add "My Circles" link to the navigation (Navbar) if not already present — check how Dashboard/Challenges nav is structured and follow that pattern
  </action>
  <verify>
    cd packages/web && npx tsc --noEmit — no TypeScript errors.
    pnpm dev — app loads without errors, /circles route renders MyCircles page, /circles/:id route renders CircleWorkspace.
  </verify>
  <done>
    My Circles list page renders contributor's circles. CircleWorkspace page shows pinned brief, member list, social link area, notes feed with infinite scroll, and note composer with file attachments. Routes registered in App.tsx. Navigation includes "My Circles" link.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes in packages/web
- /circles page renders with empty state or circle list
- /circles/:id page renders workspace with pinned brief, members, notes feed
- CircleFormationModal opens from TeamCompositionCard "Form Circle" button
- NoteComposer accepts text and file attachments
- NoteCard displays note with download links for attachments
</verification>

<success_criteria>
CM can form a Circle from team composition review and land in the workspace. Workspace shows pinned challenge brief, member list, social link area, and notes feed. Contributors can post notes with text and file attachments. Contributors can browse their Circles from /circles. All components compile and render.
</success_criteria>

<output>
After completion, create `.planning/phases/04-circles-and-collaboration/04-02-SUMMARY.md`
</output>
