---
phase: 19-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/components/a11y/RouteChangeSync.tsx
  - packages/web/src/components/layout/AppShell.tsx
  - packages/web/src/components/layout/CMRoute.tsx
  - packages/web/src/components/layout/ChallengerRoute.tsx
  - packages/web/src/components/layout/InstitutionPortalRoute.tsx
  - packages/web/src/components/layout/Navbar.tsx
  - packages/web/src/styles/app.css
autonomous: true

must_haves:
  truths:
    - "Navigating between any two pages via keyboard moves focus to the main content heading — screen readers announce the new page name"
    - "CSS animations and transitions are suppressed when the user has enabled reduce motion in their OS settings"
    - "The active Navbar link has aria-current=page and screen readers identify it as the current page"
    - "html lang=en is present on every page load"
  artifacts:
    - path: "packages/web/src/components/a11y/RouteChangeSync.tsx"
      provides: "Focus sync on SPA route change"
      exports: ["RouteChangeSync"]
    - path: "packages/web/src/styles/app.css"
      provides: "prefers-reduced-motion media query"
      contains: "prefers-reduced-motion"
  key_links:
    - from: "packages/web/src/components/layout/AppShell.tsx"
      to: "packages/web/src/components/a11y/RouteChangeSync.tsx"
      via: "JSX import and render"
      pattern: "RouteChangeSync"
    - from: "packages/web/src/components/layout/Navbar.tsx"
      to: "react-router NavLink"
      via: "NavLink with aria-current"
      pattern: "aria-current"
---

<objective>
Create the runtime accessibility foundation: SPA route-change focus sync, prefers-reduced-motion respect, aria-current on Navbar links, and lang verification.

Purpose: Every subsequent WCAG phase depends on route focus working and motion being respected. These are global concerns that affect every page.
Output: RouteChangeSync component mounted in all 4 layout shells, Navbar using NavLink with aria-current, reduced-motion CSS, confirmed lang="en".
</objective>

<execution_context>
@C:\Users\kirk_\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\kirk_\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md

@packages/web/src/components/layout/AppShell.tsx
@packages/web/src/components/layout/Navbar.tsx
@packages/web/src/components/layout/CMRoute.tsx
@packages/web/src/components/layout/ChallengerRoute.tsx
@packages/web/src/components/layout/InstitutionPortalRoute.tsx
@packages/web/src/styles/app.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: RouteChangeSync component and mount in all layout shells</name>
  <files>
    packages/web/src/components/a11y/RouteChangeSync.tsx
    packages/web/src/components/layout/AppShell.tsx
    packages/web/src/components/layout/CMRoute.tsx
    packages/web/src/components/layout/ChallengerRoute.tsx
    packages/web/src/components/layout/InstitutionPortalRoute.tsx
  </files>
  <action>
    Create `packages/web/src/components/a11y/RouteChangeSync.tsx`:
    - Import `useLocation` from `react-router`
    - In a `useEffect` keyed on `pathname`:
      1. `document.querySelector<HTMLElement>("main h1")`
      2. If found, set `h1.tabIndex = -1` then call `h1.focus({ preventScroll: false })`
    - Return `null` (no DOM output)
    - Export as named export `RouteChangeSync`

    Mount `<RouteChangeSync />` in each layout shell:

    **AppShell.tsx:** Import RouteChangeSync. Place `<RouteChangeSync />` immediately before `<Outlet />` inside the `<main>` element.

    **CMRoute.tsx:** Import RouteChangeSync. Wrap the final `return <Outlet />` as `return <><RouteChangeSync /><Outlet /></>`.

    **ChallengerRoute.tsx:** Same pattern as CMRoute — wrap with Fragment containing RouteChangeSync before Outlet.

    **InstitutionPortalRoute.tsx:** Same pattern as CMRoute — wrap with Fragment containing RouteChangeSync before Outlet.

    Note: CMRoute, ChallengerRoute, and InstitutionPortalRoute are minimal auth-gate shells (no `<main>` tag — that comes from AppShell which wraps them in the router). RouteChangeSync queries `document.querySelector("main h1")` which will find the h1 regardless of which shell it is mounted in. Mounting in all four shells ensures focus moves on every route change within any layout.
  </action>
  <verify>
    `cd packages/web && npx tsc --noEmit` passes with no errors. Visually confirm RouteChangeSync is imported and rendered in all 4 shell files.
  </verify>
  <done>RouteChangeSync component exists and is mounted in AppShell, CMRoute, ChallengerRoute, and InstitutionPortalRoute. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Navbar aria-current, reduced-motion CSS, and lang verification</name>
  <files>
    packages/web/src/components/layout/Navbar.tsx
    packages/web/src/styles/app.css
  </files>
  <action>
    **Navbar.tsx — Switch Link to NavLink for nav items:**
    - Change the import from `import { Link, useNavigate } from "react-router"` to `import { Link, NavLink, useNavigate } from "react-router"`.
    - Convert each navigation `<Link>` inside the authenticated nav section (Dashboard, Challenges, My Circles, My Impact, Wellbeing, Admin, Attention) from `<Link to={...}>` to `<NavLink to={...} end>`. Keep the `end` prop so only exact matches are active.
    - For each NavLink, use the render-prop className pattern to add active styling:
      ```
      className={({ isActive }) =>
        `${isKiosk ? "text-base" : "text-sm"} font-medium transition-colors no-underline ${
          isActive ? "text-primary-900 font-semibold" : "text-neutral-700 hover:text-primary-600"
        }`
      }
      ```
    - NavLink from react-router v7 automatically sets `aria-current="page"` on the active link. Do NOT manually add aria-current — it is built in.
    - Keep the logo `<Link>` and the footer links as `<Link>` (not NavLink) — those are not navigation items.
    - Keep the "Log in" link as `<Link>` — it's not part of the main nav.
    - Add `type="button"` to the "Enable Notifications" and "Install App" bare `<button>` elements (they already lack it).

    **app.css — Add prefers-reduced-motion:**
    Add this block at the end of the file, after the `.skip-link:focus` rule:

    ```css
    /* Respect user motion preferences (WCAG 2.3.3) */
    @media (prefers-reduced-motion: reduce) {
      *,
      *::before,
      *::after {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.01ms !important;
        scroll-behavior: auto !important;
      }
    }
    ```

    **lang verification:**
    `index.html` already has `<html lang="en">` on line 2. No change needed. Note this in the summary as confirmed.
  </action>
  <verify>
    `cd packages/web && npx tsc --noEmit` passes. Grep for `aria-current` in Navbar.tsx — should NOT appear as manual attribute (NavLink handles it automatically). Grep for `NavLink` in Navbar.tsx — should appear. Grep for `prefers-reduced-motion` in app.css — should appear. Grep for `lang="en"` in index.html — should appear.
  </verify>
  <done>All authenticated Navbar links use NavLink (auto aria-current="page"), prefers-reduced-motion CSS suppresses animations, lang="en" confirmed in index.html, bare buttons have type="button".</done>
</task>

</tasks>

<verification>
1. `cd packages/web && npx tsc --noEmit` — zero type errors
2. `cd packages/web && npx vite build` — builds successfully
3. RouteChangeSync.tsx exists in components/a11y/ and is imported in all 4 layout shells
4. Navbar.tsx uses NavLink for all 7 authenticated nav links
5. app.css contains @media (prefers-reduced-motion: reduce) block
6. index.html contains lang="en" on the html element
</verification>

<success_criteria>
- Route changes move focus to the main h1 element (RouteChangeSync mounted in all shells)
- Active nav link receives aria-current="page" automatically via NavLink
- Animations and transitions are suppressed when OS reduce-motion is enabled
- html lang="en" is confirmed present
- TypeScript and Vite build both pass
</success_criteria>

<output>
After completion, create `.planning/phases/19-foundation/19-01-SUMMARY.md`
</output>
