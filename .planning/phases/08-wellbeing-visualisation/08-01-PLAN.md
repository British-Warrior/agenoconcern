---
phase: 08-wellbeing-visualisation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/package.json
  - packages/web/src/components/wellbeing/WellbeingChart.tsx
  - packages/web/src/pages/impact/ImpactDashboard.tsx
autonomous: true

must_haves:
  truths:
    - "Impact dashboard shows SWEMWBS and UCLA scores as a multi-series line chart across all historical check-ins"
    - "Chart includes a visible UK population benchmark line for SWEMWBS at 23.6"
    - "Each score displays its band label (low / average / high) alongside the numeric value"
    - "An accessible data table of the same wellbeing scores is present for screen reader users"
    - "Empty state shows a message instead of an empty chart when no check-ins exist"
  artifacts:
    - path: "packages/web/src/components/wellbeing/WellbeingChart.tsx"
      provides: "Recharts LineChart with two series, ReferenceLine, custom Tooltip, sr-only table"
      min_lines: 80
    - path: "packages/web/src/pages/impact/ImpactDashboard.tsx"
      provides: "ImpactDashboard integrating WellbeingChart with latest-score summary"
      contains: "WellbeingChart"
  key_links:
    - from: "packages/web/src/components/wellbeing/WellbeingChart.tsx"
      to: "recharts"
      via: "named imports (LineChart, Line, ReferenceLine, ResponsiveContainer, etc.)"
      pattern: "from \"recharts\""
    - from: "packages/web/src/components/wellbeing/WellbeingChart.tsx"
      to: "packages/web/src/lib/wellbeing-norms.ts"
      via: "swemwbsBand and uclaBand for tooltip and table band labels"
      pattern: "swemwbsBand|uclaBand"
    - from: "packages/web/src/pages/impact/ImpactDashboard.tsx"
      to: "packages/web/src/components/wellbeing/WellbeingChart.tsx"
      via: "import and render passing summary.wellbeingTrajectory"
      pattern: "WellbeingChart"
---

<objective>
Replace the plain-text wellbeing trajectory list on the Impact Dashboard with an interactive recharts line chart showing SWEMWBS and UCLA scores over time, a UK population benchmark line, band labels, and an accessible companion data table.

Purpose: Make wellbeing data interpretable and funder-ready -- contributors see trend, context, and UK benchmarks rather than raw numbers.
Output: WellbeingChart component, updated ImpactDashboard, recharts installed.
</objective>

<execution_context>
@C:\Users\kirk_\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\kirk_\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-wellbeing-visualisation/08-RESEARCH.md
@packages/web/src/pages/impact/ImpactDashboard.tsx
@packages/web/src/lib/wellbeing-norms.ts
@packages/web/src/components/wellbeing/WellbeingForm.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install recharts and create WellbeingChart component</name>
  <files>
    packages/web/package.json
    packages/web/src/components/wellbeing/WellbeingChart.tsx
  </files>
  <action>
1. Install recharts in the web workspace:
   `cd packages/web && npm install recharts@^3.8.0`

2. Create `packages/web/src/components/wellbeing/WellbeingChart.tsx` with:

**Props:** `{ trajectory: WellbeingTrajectoryPoint[] }` — imported from `@indomitable-unity/shared`.

**Empty state guard:** If `trajectory.length === 0`, return a paragraph: "Your wellbeing journey will appear here after your first check-in." inside a muted Card (match existing empty state pattern: `className="opacity-60 bg-neutral-50"`).

**Data transformation:** Map `trajectory` to `chartData` array where each item has:
- `date`: formatted with `toLocaleDateString("en-GB", { day: "numeric", month: "short" })`
- `swemwbs`: `p.wemwbsScore`
- `ucla`: `p.uclaScore`
- `completedAt`: raw ISO string (retained for table)

**Chart section** (wrap in `<div className="h-72" aria-hidden="true">`):
- `ResponsiveContainer width="100%" height="100%"` wrapping a `LineChart`
- `CartesianGrid strokeDasharray="3 3" stroke="#e5e5e5"`
- `XAxis dataKey="date"` with `tick={{ fontSize: 11 }}`
- `YAxis domain={[0, 40]}` with `tick={{ fontSize: 11 }}` — single axis (UCLA compressed but acceptable per research decision)
- `Legend` — default position
- `ReferenceLine y={23.6} stroke="#d97706" strokeDasharray="4 4"` with label `{ value: "UK mean (23.6)", position: "right", fontSize: 10, fill: "#d97706" }`
- `Line type="monotone" dataKey="swemwbs" name="SWEMWBS" stroke="#1e3a5f" strokeWidth={2} dot={{ r: 4 }} activeDot={{ r: 6 }}`
- `Line type="monotone" dataKey="ucla" name="UCLA (loneliness)" stroke="#f59e0b" strokeWidth={2} dot={{ r: 4 }} activeDot={{ r: 6 }}`
- Custom `Tooltip` via `content` prop: render a small card showing date, SWEMWBS score + band label (from `swemwbsBand`), UCLA score + band label (from `uclaBand`). Use band `.color` class for label text. Only render when `active && payload?.length`.

**Do NOT** set `accessibilityLayer` prop — it defaults to `true` in recharts 3.x. Do NOT use deprecated props `alwaysShow`, `isFront`, or chart refs.

**Companion accessible table** (immediately after the chart div):
- `<table className="sr-only" aria-label="Wellbeing scores by date">`
- `<caption>` explaining both scales (SWEMWBS higher=better, UCLA lower=better)
- Columns: Date, SWEMWBS score (out of 35), SWEMWBS band, UCLA score (out of 12), UCLA band
- Each row maps a trajectory point, using `swemwbsBand` and `uclaBand` for band label text
- Date format in table: `day: "numeric", month: "short", year: "numeric"` (more context than chart x-axis)
- Use `className="sr-only"` (NOT `hidden` attribute — `hidden` removes from accessibility tree)

Export the component as a named export: `export function WellbeingChart`.
  </action>
  <verify>
    `cd packages/web && npx tsc --noEmit` passes with no errors.
    `grep "recharts" packages/web/package.json` shows recharts in dependencies.
    WellbeingChart.tsx exists and exports WellbeingChart.
  </verify>
  <done>
    WellbeingChart component exists with: LineChart (two series), ReferenceLine at 23.6, custom Tooltip with band labels, empty state guard, sr-only companion table. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate WellbeingChart into ImpactDashboard</name>
  <files>
    packages/web/src/pages/impact/ImpactDashboard.tsx
  </files>
  <action>
1. Import `WellbeingChart` from `../../components/wellbeing/WellbeingChart.js` at the top of ImpactDashboard.tsx.

2. Replace the existing `WellbeingSection` component (lines ~310-396) and its usage in the JSX with a new section that contains:

   a. A `Card` wrapper (matching existing layout) with `className="lg:col-span-2"` on the outer div (preserving the current full-width layout).

   b. **Header row:** "Wellbeing Trajectory" heading (h2, same style as existing: `text-lg font-semibold text-neutral-700 mb-4`).

   c. **Latest score summary** (only when `trajectory.length > 0`): Show the most recent check-in's SWEMWBS and UCLA scores with their band labels and trend arrows. Reuse the existing trend arrow logic (SWEMWBS: up=green, down=red; UCLA: down=green, up=red — inverted per Phase 7 decision). Keep the `trendDirection` import from `wellbeing-norms.js`. Display as a flex row above the chart:
      - "SWEMWBS: {score}/35 {bandLabel} {trendArrow}"
      - "UCLA: {score}/12 {bandLabel} {trendArrow}"

   d. **Chart:** `<WellbeingChart trajectory={summary.wellbeingTrajectory} />` — this handles the chart, empty state, and sr-only table internally.

   e. **Footer note** (keep existing): the explanatory text about SWEMWBS and UCLA scale ranges.

3. Remove the old `WellbeingSection` function entirely — it is fully replaced.

4. Clean up any unused imports that were only used by the old WellbeingSection (check if `WellbeingTrajectoryPoint` type is still needed in ImpactDashboard — it may still be used for the latest-score summary; keep if so).

Do NOT add a new `useWellbeingHistory` import — use `summary.wellbeingTrajectory` from the existing `useImpactSummary` call (zero new hooks per research).
  </action>
  <verify>
    `cd packages/web && npx tsc --noEmit` passes.
    `cd packages/web && npm run build` succeeds.
    Grep confirms: no remaining reference to old `WellbeingSection` function, `WellbeingChart` is imported and used, no `useWellbeingHistory` import in ImpactDashboard.
  </verify>
  <done>
    ImpactDashboard renders WellbeingChart with trajectory data. Latest scores show band labels and trend arrows above the chart. Old WellbeingSection removed. Build passes cleanly.
  </done>
</task>

</tasks>

<verification>
1. `cd packages/web && npm run build` — full production build succeeds
2. `npx tsc --noEmit` in packages/web — no type errors
3. `grep -r "WellbeingSection" packages/web/src/` — returns zero results (old component fully removed)
4. `grep -r "useWellbeingHistory" packages/web/src/pages/impact/` — returns zero results (no unnecessary hook)
5. `grep "sr-only" packages/web/src/components/wellbeing/WellbeingChart.tsx` — confirms accessible table present
6. `grep "ReferenceLine" packages/web/src/components/wellbeing/WellbeingChart.tsx` — confirms benchmark line present
7. `grep "recharts" packages/web/package.json` — confirms recharts installed
</verification>

<success_criteria>
- Impact dashboard renders a multi-series recharts LineChart with SWEMWBS and UCLA lines
- UK SWEMWBS population mean (23.6) visible as a dashed reference line
- Custom tooltip shows score + band label on hover
- Latest scores above chart display numeric value + band label + trend arrow
- Screen-reader-accessible data table with sr-only class contains all trajectory data with band labels
- Empty state handled gracefully (no empty chart axes)
- Zero new API hooks — all data from existing useImpactSummary
- Production build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-wellbeing-visualisation/08-01-SUMMARY.md`
</output>
