---
phase: 06-wellbeing-notifications-and-pwa
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/db/schema.ts
  - packages/shared/src/types/notifications.ts
  - packages/shared/src/schemas/notifications.ts
  - packages/shared/src/index.ts
  - packages/server/src/services/notification.service.ts
  - packages/server/src/routes/notifications.ts
  - packages/server/src/express-app.ts
  - packages/server/src/config/env.ts
  - packages/server/package.json
  - packages/web/src/api/notifications.ts
  - packages/web/src/hooks/useNotifications.ts
  - packages/web/src/components/layout/NotificationBell.tsx
  - packages/web/src/components/layout/Navbar.tsx
  - packages/server/src/services/wellbeing-reminder.job.ts
  - packages/server/src/routes/challenges.ts
  - packages/server/src/routes/circles.ts
  - packages/server/src/routes/payments.ts
  - packages/server/src/index.ts
autonomous: true

user_setup:
  - service: web-push VAPID
    why: "Push notification encryption requires VAPID key pair"
    env_vars:
      - name: VAPID_PUBLIC_KEY
        source: "Generate once via: npx web-push generate-vapid-keys"
      - name: VAPID_PRIVATE_KEY
        source: "Same command as above — store private key securely"
      - name: VAPID_SUBJECT
        source: "Set to mailto:support@indomitableunity.org (or actual contact email)"
    dashboard_config: []

must_haves:
  truths:
    - "User receives in-app notifications for challenge matches, Circle formation, Circle activity, wellbeing reminders, resolution feedback, and payments"
    - "Notifications are stored persistently and retrievable as a list with read/unread status"
    - "User can mark notifications as read"
    - "Circle activity notification frequency is configurable (immediate, daily digest, off)"
    - "Push notifications sent via web-push with email fallback via Resend when push fails or no subscription exists"
    - "Wellbeing reminder notifications fire automatically via scheduled job for overdue contributors"
    - "notify() is called from challenge match, Circle formation, Circle activity, and payment routes"
  artifacts:
    - path: "packages/server/src/db/schema.ts"
      provides: "notifications table, pushSubscriptions table, notifyCircleActivityEnum enum, notifyCircleActivity column on contributorProfiles"
      contains: "notifications"
    - path: "packages/server/src/services/notification.service.ts"
      provides: "notify() function that tries push then falls back to email"
      exports: ["notify", "notifyBatch"]
    - path: "packages/server/src/routes/notifications.ts"
      provides: "GET /api/notifications, PATCH /api/notifications/:id/read, POST /api/notifications/push-sub, DELETE /api/notifications/push-sub, PATCH /api/notifications/preferences"
      exports: ["notificationRoutes"]
    - path: "packages/shared/src/types/notifications.ts"
      provides: "NotificationType, Notification, NotificationPreferences, PushSubscriptionInput types"
    - path: "packages/web/src/components/layout/NotificationBell.tsx"
      provides: "Bell icon with unread count badge and dropdown list"
    - path: "packages/server/src/services/wellbeing-reminder.job.ts"
      provides: "Scheduled cron job querying overdue contributors and calling notifyBatch with wellbeing_reminder type"
      exports: ["startWellbeingReminderJob"]
  key_links:
    - from: "packages/server/src/services/notification.service.ts"
      to: "pushSubscriptions table"
      via: "Looks up push subscription for contributor, sends via web-push"
      pattern: "webpush\\.sendNotification"
    - from: "packages/server/src/services/notification.service.ts"
      to: "resend"
      via: "Email fallback when push fails or no subscription"
      pattern: "resend\\.emails\\.send"
    - from: "packages/web/src/components/layout/NotificationBell.tsx"
      to: "/api/notifications"
      via: "GET to list notifications, PATCH to mark read"
      pattern: "fetch.*api/notifications"
    - from: "packages/server/src/services/wellbeing-reminder.job.ts"
      to: "packages/server/src/services/notification.service.ts"
      via: "Calls notifyBatch with type wellbeing_reminder for overdue contributors"
      pattern: "notifyBatch.*wellbeing_reminder"
    - from: "packages/server/src/routes/challenges.ts"
      to: "packages/server/src/services/notification.service.ts"
      via: "Calls notify with type challenge_match on match creation"
      pattern: "notify.*challenge_match"
---

<objective>
Build the notification system with persistent storage, push delivery via web-push, email fallback via Resend, and in-app notification bell UI.

Purpose: Keep contributors informed about challenge matches, Circle activity, wellbeing reminders, resolution feedback, and payments through a unified notification system with configurable delivery preferences.
Output: notifications + pushSubscriptions DB tables, notification service with push + email, REST routes, NotificationBell component in navbar, notification preferences for Circle activity.
</objective>

<execution_context>
@C:\Users\kirk_\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\kirk_\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-wellbeing-notifications-and-pwa/06-RESEARCH.md

@packages/server/src/db/schema.ts
@packages/server/src/express-app.ts
@packages/server/src/config/env.ts
@packages/server/package.json
@packages/shared/src/index.ts
@packages/web/src/components/layout/Navbar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Notification DB schema, shared types, notification service, and server routes</name>
  <files>
    packages/server/src/db/schema.ts
    packages/shared/src/types/notifications.ts
    packages/shared/src/schemas/notifications.ts
    packages/shared/src/index.ts
    packages/server/src/services/notification.service.ts
    packages/server/src/routes/notifications.ts
    packages/server/src/express-app.ts
    packages/server/src/config/env.ts
    packages/server/package.json
  </files>
  <action>
**1. Install web-push** — Run `npm install web-push` and `npm install -D @types/web-push` in packages/server.

**2. Env config** — Add to packages/server/src/config/env.ts envSchema:
- VAPID_PUBLIC_KEY: z.string().default('')
- VAPID_PRIVATE_KEY: z.string().default('')
- VAPID_SUBJECT: z.string().default('mailto:support@indomitableunity.org')

**3. DB schema additions** in packages/server/src/db/schema.ts:

Add `notifyCircleActivityEnum` enum:
```
pgEnum('notify_circle_activity', ['immediate', 'daily_digest', 'off'])
```

Add `notifyCircleActivity` column to `contributorProfiles` table:
```
notifyCircleActivity: notifyCircleActivityEnum('notify_circle_activity').default('immediate')
```

Add `pushSubscriptions` table:
- id: uuid PK defaultRandom
- contributorId: uuid FK to contributors.id, onDelete cascade, NOT NULL
- endpoint: text NOT NULL, unique
- p256dh: text NOT NULL
- auth: text NOT NULL
- createdAt: timestamp with timezone, defaultNow, NOT NULL
- updatedAt: timestamp with timezone, defaultNow, NOT NULL

Add `notificationTypeEnum`:
```
pgEnum('notification_type', ['challenge_match', 'circle_formed', 'circle_activity', 'wellbeing_reminder', 'resolution_feedback', 'payment_received'])
```

Add `notifications` table:
- id: uuid PK defaultRandom
- contributorId: uuid FK to contributors.id, onDelete cascade, NOT NULL
- type: notificationTypeEnum NOT NULL
- title: text NOT NULL
- body: text NOT NULL
- url: text (nullable — deep link)
- readAt: timestamp with timezone (nullable)
- createdAt: timestamp with timezone, defaultNow, NOT NULL

**4. Shared types** — Create packages/shared/src/types/notifications.ts:
```typescript
export type NotificationType = 'challenge_match' | 'circle_formed' | 'circle_activity' | 'wellbeing_reminder' | 'resolution_feedback' | 'payment_received';
export type NotifyCircleActivity = 'immediate' | 'daily_digest' | 'off';

export interface Notification {
  id: string;
  type: NotificationType;
  title: string;
  body: string;
  url: string | null;
  readAt: string | null;
  createdAt: string;
}

export interface NotificationPreferences {
  notifyCircleActivity: NotifyCircleActivity;
}

export interface PushSubscriptionInput {
  endpoint: string;
  keys: {
    p256dh: string;
    auth: string;
  };
}
```

**5. Shared schemas** — Create packages/shared/src/schemas/notifications.ts:
- pushSubscriptionSchema: validates endpoint (string URL), keys.p256dh (string), keys.auth (string)
- notificationPreferencesSchema: validates notifyCircleActivity as enum

**6. Export from shared index** — Add notification types and schemas to packages/shared/src/index.ts

**7. Notification service** — Create packages/server/src/services/notification.service.ts:

```typescript
import webpush from 'web-push';
import { Resend } from 'resend';
```

- On module load: if VAPID_PUBLIC_KEY and VAPID_PRIVATE_KEY are non-empty, call `webpush.setVapidDetails(VAPID_SUBJECT, VAPID_PUBLIC_KEY, VAPID_PRIVATE_KEY)`. If empty, log warning and skip VAPID setup (graceful degradation).

- `async function notify(contributorId: string, payload: { type: NotificationType, title: string, body: string, url?: string })`:
  1. Insert into notifications table (always — this is the in-app record)
  2. Look up all pushSubscriptions for contributorId
  3. For each subscription, try `webpush.sendNotification(sub, JSON.stringify({ title, body, url }))`
     - On 410 or 404 status error: delete that push subscription row from DB
     - On other errors: log and continue
  4. If NO push subscriptions exist OR all push sends failed:
     - Look up contributor email from contributors table
     - If RESEND_API_KEY is non-empty and email exists, send email via Resend: `resend.emails.send({ from: 'Indomitable Unity <noreply@indomitableunity.org>', to: [email], subject: title, html: '<p>' + body + '</p>' + (url ? '<p><a href="' + CLIENT_URL + url + '">View</a></p>' : '') })`
     - If RESEND_API_KEY is empty, log warning (graceful degradation)

- `async function notifyBatch(contributorIds: string[], payload)`: calls notify for each (sequential to avoid rate limits).

**8. Server routes** — Create packages/server/src/routes/notifications.ts:

`GET /api/notifications` (auth required):
- Query notifications for contributorId, order by createdAt desc, limit 50
- Return array of Notification objects

`PATCH /api/notifications/:id/read` (auth required):
- Update readAt to now() where id = params.id AND contributorId = auth user
- Return 200

`POST /api/notifications/read-all` (auth required):
- Update readAt to now() for all unread notifications for contributorId
- Return 200

`POST /api/notifications/push-sub` (auth required):
- Parse body with pushSubscriptionSchema
- Upsert into pushSubscriptions: insert on conflict (endpoint) update p256dh, auth, updatedAt
- Return 201

`DELETE /api/notifications/push-sub` (auth required):
- Parse body.endpoint
- Delete from pushSubscriptions where endpoint and contributorId match
- Return 200

`GET /api/notifications/preferences` (auth required):
- Query contributorProfiles.notifyCircleActivity for contributorId
- Return { notifyCircleActivity } (default 'immediate' if null)

`PATCH /api/notifications/preferences` (auth required):
- Parse body with notificationPreferencesSchema
- Update contributorProfiles.notifyCircleActivity for contributorId
- Return 200

**9. Wire routes** — In packages/server/src/express-app.ts:
- Import notificationRoutes from routes/notifications.ts
- Mount at `/api/notifications`
  </action>
  <verify>
- `npm install` succeeds in packages/server (web-push installed)
- `npx tsc --noEmit` passes in packages/server and packages/shared
- Verify schema.ts has notifications, pushSubscriptions tables and notifyCircleActivity column
- Verify notification.service.ts exports notify and notifyBatch
- Verify notifications.ts routes export a Router with all endpoints
  </verify>
  <done>
- notifications and pushSubscriptions tables in schema
- notifyCircleActivity column on contributorProfiles with 'immediate'/'daily_digest'/'off' enum
- Notification service tries push via web-push, falls back to email via Resend, handles 410 subscription cleanup
- 7 REST endpoints for notification list, mark read, push subscription CRUD, preferences
- VAPID env vars added with graceful degradation when empty
  </done>
</task>

<task type="auto">
  <name>Task 2: NotificationBell component and navbar integration</name>
  <files>
    packages/web/src/api/notifications.ts
    packages/web/src/hooks/useNotifications.ts
    packages/web/src/components/layout/NotificationBell.tsx
    packages/web/src/components/layout/Navbar.tsx
  </files>
  <action>
**1. API client** — Create packages/web/src/api/notifications.ts:
- getNotifications(): GET /api/notifications → Notification[]
- markRead(id: string): PATCH /api/notifications/:id/read
- markAllRead(): POST /api/notifications/read-all
- savePushSubscription(sub: PushSubscriptionInput): POST /api/notifications/push-sub
- removePushSubscription(endpoint: string): DELETE /api/notifications/push-sub
- getPreferences(): GET /api/notifications/preferences → NotificationPreferences
- updatePreferences(prefs: NotificationPreferences): PATCH /api/notifications/preferences

**2. Hooks** — Create packages/web/src/hooks/useNotifications.ts:
- useNotifications(): TanStack useQuery for notification list, refetchInterval 60_000 (poll every 60s for new notifications)
- useMarkRead(): useMutation, invalidates notifications query on success
- useMarkAllRead(): useMutation, invalidates notifications query on success
- useNotificationPreferences(): useQuery for preferences
- useUpdatePreferences(): useMutation, invalidates preferences query

**3. NotificationBell component** — Create packages/web/src/components/layout/NotificationBell.tsx:

A bell icon button in the navbar that shows:
- Bell icon (use a simple SVG bell — inline, no icon library needed)
- Unread count badge (small red circle with number, positioned top-right of bell) — count of notifications where readAt is null. Hide badge if count is 0.
- On click: toggle a dropdown panel below the bell

Dropdown panel:
- Header row: "Notifications" title + "Mark all read" text button (right-aligned)
- Scrollable list (max-height ~320px) of notification items
- Each item: title (bold), body (truncated to 1 line), relative time (e.g. "2h ago", "3d ago" — compute from createdAt)
- Unread items have a subtle left border accent colour (use primary from OKLCH palette)
- Click on item: mark as read (call useMarkRead), then navigate to item.url if present
- Empty state: "No notifications yet"
- Close dropdown when clicking outside (use a simple useEffect with document click listener)

**Styling**: Tailwind utility classes. Dropdown has white background, shadow-lg, rounded-lg, w-80. Items have hover:bg-stone-50. Badge uses bg-red-500 text-white text-xs rounded-full.

**4. Navbar integration** — In packages/web/src/components/layout/Navbar.tsx:
- Import and render <NotificationBell /> next to the existing user menu / nav links
- Only render for authenticated users (check if auth user exists)
- Position bell before the user name/logout area
  </action>
  <verify>
- `npx tsc --noEmit` passes in packages/web
- Verify NotificationBell.tsx renders bell with badge and dropdown
- Verify Navbar.tsx includes NotificationBell for authenticated users
- Verify notifications hook polls every 60 seconds
  </verify>
  <done>
- NotificationBell shows unread count badge and dropdown with notification list
- Click notification marks as read and navigates to deep link
- "Mark all read" clears all unread badges
- Navbar includes NotificationBell for authenticated users
- Polling refreshes notifications every 60 seconds
  </done>
</task>

<task type="auto">
  <name>Task 3: Scheduled wellbeing_reminder job and wire notify() into existing business event routes</name>
  <files>
    packages/server/src/services/wellbeing-reminder.job.ts
    packages/server/src/routes/challenges.ts
    packages/server/src/routes/circles.ts
    packages/server/src/routes/payments.ts
    packages/server/src/index.ts
    packages/server/package.json
  </files>
  <action>
**1. Install node-cron** — Run in packages/server:
```bash
npm install node-cron && npm install -D @types/node-cron
```

**2. Create wellbeing reminder scheduled job** — Create `packages/server/src/services/wellbeing-reminder.job.ts`:
- Import `cron` from `node-cron`, `db` from db module, `notifyBatch` from notification.service
- Export `startWellbeingReminderJob()` function that schedules a cron job running daily at 09:00 UTC (`'0 9 * * *'`)
- Job logic:
  1. Query all contributors who have at least one wellbeingCheckin but whose most recent completedAt is older than 56 days (8 weeks)
  2. Also include contributors who completed onboarding (have a contributor record) but have zero wellbeingCheckins and whose account was created > 56 days ago
  3. Collect their contributorIds
  4. Call `notifyBatch(contributorIds, { type: 'wellbeing_reminder', title: 'Wellbeing check-in due', body: 'It has been 8 weeks since your last wellbeing check-in. Complete it now to track your wellbeing journey.', url: '/wellbeing/checkin' })`
  5. Log the count of reminders sent
- Add error handling: wrap in try/catch, log errors but do not crash the server

**3. Start the job in server entrypoint** — In `packages/server/src/index.ts` (or wherever the Express server starts listening):
- Import `startWellbeingReminderJob` from services/wellbeing-reminder.job
- Call `startWellbeingReminderJob()` after the server starts listening (inside the `.listen()` callback)

**4. Wire notify() into existing business event routes** — Add notify() calls to existing server routes where business events occur. For each route, import `notify` from notification.service and call it after the successful business action:

**challenges.ts** — When a challenge match is confirmed/created (the endpoint that pairs two contributors):
- Call `notify(matchedContributorId, { type: 'challenge_match', title: 'New challenge match!', body: 'You have been matched for a challenge. View details and connect with your match.', url: '/challenges/{challengeId}' })`
- Notify BOTH matched contributors

**circles.ts** — When a Circle is formed (enough members join):
- Call `notifyBatch(memberIds, { type: 'circle_formed', title: 'Your Circle is ready!', body: 'Your Circle has been formed. Meet your fellow members and get started.', url: '/circles/{circleId}' })`
- When a Circle activity occurs (e.g., a message/update is posted):
- Check each member's notifyCircleActivity preference: if 'immediate', call notify() immediately. If 'daily_digest' or 'off', skip (daily_digest is a future enhancement — for now treat same as 'off').

**payments.ts** — When a payment is received/confirmed:
- Call `notify(contributorId, { type: 'payment_received', title: 'Payment received', body: 'Your payment has been processed successfully.', url: '/payments' })`

**Note:** resolution_feedback notifications will be wired when the resolution system is built (future phase). For now, the type exists in the enum and service but has no trigger — this is intentional infrastructure.
  </action>
  <verify>
- `npx tsc --noEmit` passes in packages/server
- Verify wellbeing-reminder.job.ts exports startWellbeingReminderJob with node-cron schedule
- Verify challenges.ts, circles.ts, payments.ts import and call notify/notifyBatch
- Verify server entrypoint calls startWellbeingReminderJob()
  </verify>
  <done>
- Scheduled job runs daily at 09:00 UTC, queries overdue contributors (>56 days since last wellbeing check-in), and sends wellbeing_reminder notifications via notifyBatch
- challenge_match notifications fire when matches are created
- circle_formed notifications fire when Circles form
- circle_activity notifications respect per-user preference (immediate vs off)
- payment_received notifications fire on payment confirmation
- resolution_feedback type exists as infrastructure for future wiring
  </done>
</task>

</tasks>

<verification>
1. Install web-push in packages/server and verify import resolves
2. Verify notifications table and pushSubscriptions table in schema
3. POST /api/notifications/push-sub stores subscription correctly
4. notify() function inserts notification record and attempts push (graceful failure without VAPID keys)
5. GET /api/notifications returns notification list ordered by createdAt desc
6. NotificationBell renders in navbar with unread count and dropdown
7. Circle activity preferences can be read and updated via API
</verification>

<success_criteria>
- All 6 notification types (challenge_match, circle_formed, circle_activity, wellbeing_reminder, resolution_feedback, payment_received) supported
- Push delivery via web-push with 410 subscription cleanup
- Email fallback via Resend when push unavailable
- In-app notification list with read/unread state
- Circle activity notifications configurable (immediate/daily_digest/off) per NOTF-03
- NotificationBell integrated into navbar for all authenticated users
</success_criteria>

<output>
After completion, create `.planning/phases/06-wellbeing-notifications-and-pwa/06-02-SUMMARY.md`
</output>
