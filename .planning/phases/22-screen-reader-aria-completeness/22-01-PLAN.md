---
phase: 22-screen-reader-aria-completeness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/pages/Register.tsx
  - packages/web/src/components/layout/NotificationBell.tsx
  - packages/web/src/pages/Dashboard.tsx
  - packages/web/src/pages/challenger/ChallengerDashboard.tsx
  - packages/web/src/pages/impact/ImpactDashboard.tsx
  - packages/web/src/pages/admin/ContributorDetail.tsx
  - packages/web/src/pages/challenger/ChallengeDetail.tsx
  - packages/web/src/pages/institution/InstitutionLanding.tsx
  - packages/web/src/pages/admin/InstitutionManagement.tsx
  - packages/web/src/pages/portal/PortalDashboard.tsx
autonomous: true

must_haves:
  truths:
    - "When a form validation error appears on the Register privacy checkbox, a screen reader announces the error via aria-describedby linkage"
    - "When unread notification count increases, a screen reader announces the new count without user interaction"
    - "Skeleton loading placeholders are identified as loading state by screen readers via role=status and aria-busy=true"
  artifacts:
    - path: "packages/web/src/pages/Register.tsx"
      provides: "aria-describedby on privacy checkbox linking to error message"
      contains: "aria-describedby"
    - path: "packages/web/src/components/layout/NotificationBell.tsx"
      provides: "useAnnounce call on unread count increase"
      contains: "useAnnounce"
    - path: "packages/web/src/pages/Dashboard.tsx"
      provides: "role=status and aria-busy on skeleton"
      contains: "aria-busy"
    - path: "packages/web/src/pages/challenger/ChallengerDashboard.tsx"
      provides: "role=status and aria-busy on skeleton"
      contains: "aria-busy"
    - path: "packages/web/src/pages/impact/ImpactDashboard.tsx"
      provides: "role=status and aria-busy on skeleton"
      contains: "aria-busy"
    - path: "packages/web/src/pages/admin/ContributorDetail.tsx"
      provides: "role=status and aria-busy on skeleton"
      contains: "aria-busy"
    - path: "packages/web/src/pages/challenger/ChallengeDetail.tsx"
      provides: "role=status and aria-busy on skeleton"
      contains: "aria-busy"
    - path: "packages/web/src/pages/institution/InstitutionLanding.tsx"
      provides: "role=status and aria-busy on skeleton"
      contains: "aria-busy"
    - path: "packages/web/src/pages/admin/InstitutionManagement.tsx"
      provides: "role=status and aria-busy on skeleton"
      contains: "aria-busy"
    - path: "packages/web/src/pages/portal/PortalDashboard.tsx"
      provides: "aria-busy on existing role=status skeleton"
      contains: "aria-busy"
  key_links:
    - from: "packages/web/src/pages/Register.tsx"
      to: "privacy checkbox error <p>"
      via: "aria-describedby pointing to error id"
      pattern: "aria-describedby.*privacy"
    - from: "packages/web/src/components/layout/NotificationBell.tsx"
      to: "AnnounceProvider"
      via: "useAnnounce hook"
      pattern: "useAnnounce"
---

<objective>
Close the small-fix ARIA gaps: link the Register privacy checkbox error to its input (SR-02), announce badge count increases to screen readers (SR-04), and add role="status" + aria-busy="true" to all skeleton loading states (SR-05). SR-03 (dropzone label) is already implemented — no work needed. FORM-01 and FORM-02 are already correct — no work needed.

Purpose: Screen reader users get programmatic access to form errors, dynamic badge changes, and loading states.
Output: Updated Register.tsx, NotificationBell.tsx, and 8 skeleton-containing page files.
</objective>

<execution_context>
@C:\Users\kirk_\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\kirk_\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-screen-reader-aria-completeness/22-RESEARCH.md
@packages/web/src/pages/Register.tsx
@packages/web/src/components/layout/NotificationBell.tsx
@packages/web/src/a11y/AnnounceProvider.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: SR-02 privacy checkbox error linking + SR-04 badge count announcement</name>
  <files>
    packages/web/src/pages/Register.tsx
    packages/web/src/components/layout/NotificationBell.tsx
  </files>
  <action>
**Register.tsx — SR-02 privacy checkbox aria-describedby:**

1. Find the privacy consent `<input type="checkbox" id="privacy-consent">` (around line 160).
2. Add `aria-describedby={fieldErrors.privacy ? "privacy-consent-error" : undefined}` to the checkbox.
3. Add `aria-invalid={fieldErrors.privacy ? true : undefined}` to the checkbox.
4. On the error `<p>` element that renders `fieldErrors.privacy` (around line 182), add `id="privacy-consent-error"`. The `<p>` already has `role="alert"` — keep that.

No other forms need changes. The `Input.tsx` wrapper already handles aria-describedby for all standard inputs. The resolve-notes textarea in AttentionDashboard does not render an error message, so no aria-describedby is needed there.

**NotificationBell.tsx — SR-04 badge count announcement:**

1. Import `useAnnounce` from `"../../a11y/AnnounceProvider.js"`.
2. Inside the `NotificationBell` component, after the existing `unreadCount` derivation (line 30):
   - Call `const announce = useAnnounce();`
   - Add `const prevCountRef = useRef<number>(unreadCount);` (useRef is already imported)
   - Add a `useEffect` that:
     - Checks if `unreadCount > prevCountRef.current` (only announce increases, NOT decreases)
     - If so, calls `announce(\`${unreadCount} unread notification${unreadCount !== 1 ? "s" : ""}\`)`
     - Always updates `prevCountRef.current = unreadCount`
     - Dependencies: `[unreadCount, announce]`
3. Do NOT announce when count decreases (user marking items read). Only announce new notifications arriving.
  </action>
  <verify>
Run `cd packages/web && npx tsc --noEmit` — zero type errors.
Run `cd packages/web && npm run lint` — zero new lint errors.
Grep Register.tsx for `aria-describedby` on the checkbox — confirm it references `privacy-consent-error`.
Grep NotificationBell.tsx for `useAnnounce` — confirm the import and usage exist.
Grep NotificationBell.tsx for `prevCountRef` — confirm previous-count tracking exists.
  </verify>
  <done>
Register privacy checkbox has aria-describedby pointing to its error message when error is present. NotificationBell calls useAnnounce when unread count increases, and does not announce on decrease.
  </done>
</task>

<task type="auto">
  <name>Task 2: SR-05 skeleton loading states — add role="status" and aria-busy to all skeletons</name>
  <files>
    packages/web/src/pages/Dashboard.tsx
    packages/web/src/pages/challenger/ChallengerDashboard.tsx
    packages/web/src/pages/impact/ImpactDashboard.tsx
    packages/web/src/pages/admin/ContributorDetail.tsx
    packages/web/src/pages/challenger/ChallengeDetail.tsx
    packages/web/src/pages/institution/InstitutionLanding.tsx
    packages/web/src/pages/admin/InstitutionManagement.tsx
    packages/web/src/pages/portal/PortalDashboard.tsx
  </files>
  <action>
For each file, find the skeleton/loading placeholder (either a local `SkeletonCard` component or inline `animate-pulse` divs) and add ARIA attributes to the outermost skeleton wrapper:

**Pattern for each skeleton wrapper div:**
- Add `role="status"` (if not already present)
- Add `aria-busy="true"`
- Add `aria-label="Loading"`
- Add a `<span className="sr-only">Loading...</span>` as the first child inside the wrapper (if no sr-only loading text already exists)

**Files and specifics:**

1. **Dashboard.tsx** — Local `SkeletonCard` function. Add attrs to its root `<div>`. Also check if the page wraps multiple SkeletonCards in a container — if so, put role/aria-busy on each card individually, NOT on the outer grid.

2. **ChallengerDashboard.tsx** — Local `SkeletonCard` function. Same pattern as Dashboard.

3. **ImpactDashboard.tsx** — Local `SkeletonCard` function. Same pattern.

4. **ContributorDetail.tsx** — Inline `animate-pulse` block. Add role/aria-busy to the outermost loading wrapper div.

5. **ChallengeDetail.tsx** — Inline `animate-pulse` block. Same pattern.

6. **InstitutionLanding.tsx** — Inline `animate-pulse` block. Same pattern.

7. **InstitutionManagement.tsx** — Multiple inline `animate-pulse` text elements. Find the outermost loading container and add role/aria-busy there, with a single sr-only Loading span.

8. **PortalDashboard.tsx** — Already has `role="status"` on its skeleton divs. Add `aria-busy="true"` and an sr-only `Loading...` span if not present.

**Important:** Do NOT add `aria-busy="false"` anywhere — skeletons are conditionally rendered and removed from DOM when loading finishes. Do NOT consolidate skeleton components into a shared file — add attrs in-place. Keep changes minimal: only add the ARIA attributes and sr-only span.
  </action>
  <verify>
Run `cd packages/web && npx tsc --noEmit` — zero type errors.
Run `cd packages/web && npm run lint` — zero new lint errors.
Run grep across all 8 files for `aria-busy="true"` — confirm each file has at least one occurrence.
Run grep across all 8 files for `role="status"` — confirm each file has at least one occurrence.
  </verify>
  <done>
All 8 skeleton-containing pages have role="status", aria-busy="true", and sr-only "Loading..." text on their skeleton wrappers. Screen readers identify these as loading states.
  </done>
</task>

</tasks>

<verification>
1. `cd packages/web && npx tsc --noEmit` passes with zero errors
2. `cd packages/web && npm run lint` passes with zero errors
3. Register.tsx: privacy checkbox has `aria-describedby` and `aria-invalid` when error present
4. NotificationBell.tsx: calls `useAnnounce()` on count increase only
5. All 8 skeleton files have `role="status"` + `aria-busy="true"`
</verification>

<success_criteria>
- SR-02: Register privacy checkbox error is linked via aria-describedby (the only real gap — Input.tsx already handles all other form inputs)
- SR-03: Already implemented (UploadCV.tsx) — confirmed, no changes
- SR-04: NotificationBell announces unread count increases via useAnnounce; does not announce decreases
- SR-05: All 8 skeleton loading states have role="status", aria-busy="true", and sr-only Loading text
- FORM-01: Already correct — confirmed, no changes
- FORM-02: Already correct — confirmed, no changes
- TypeScript and lint pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/22-screen-reader-aria-completeness/22-01-SUMMARY.md`
</output>
