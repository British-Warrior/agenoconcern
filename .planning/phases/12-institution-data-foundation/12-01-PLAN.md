---
phase: 12-institution-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/scripts/create-contributor-institutions.mjs
  - packages/server/src/db/schema.ts
  - packages/shared/src/schemas/institution.ts
  - packages/shared/src/index.ts
autonomous: true

must_haves:
  truths:
    - "contributor_institutions junction table exists in PostgreSQL with composite unique constraint"
    - "Drizzle schema exports contributorInstitutions with proper FK references"
    - "Zod schemas for institution CRUD and contributor assignment exist in shared package"
  artifacts:
    - path: "packages/server/scripts/create-contributor-institutions.mjs"
      provides: "Junction table migration script"
      contains: "contributor_institutions"
    - path: "packages/server/src/db/schema.ts"
      provides: "contributorInstitutions Drizzle table definition"
      exports: ["contributorInstitutions"]
    - path: "packages/shared/src/schemas/institution.ts"
      provides: "Zod validation schemas for institution CRUD"
      exports: ["createInstitutionSchema", "updateInstitutionSchema"]
  key_links:
    - from: "packages/server/src/db/schema.ts"
      to: "packages/server/scripts/create-contributor-institutions.mjs"
      via: "schema matches migration SQL"
      pattern: "contributor_institutions"
---

<objective>
Create the contributor_institutions many-to-many junction table, update the Drizzle schema, and add shared Zod validation schemas for institution management.

Purpose: Foundation for all Phase 12 work — the junction table enables contributor-institution assignments, and the Zod schemas ensure consistent validation across API routes.
Output: Migration script, updated Drizzle schema, shared institution schemas.
</objective>

<execution_context>
@C:\Users\kirk_\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\kirk_\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-institution-data-foundation/12-RESEARCH.md
@.planning/phases/12-institution-data-foundation/12-CONTEXT.md
@packages/server/src/db/schema.ts
@packages/server/scripts/create-institutions-table.mjs
@packages/shared/src/schemas/circle.ts
@packages/shared/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create junction table migration and update Drizzle schema</name>
  <files>
    packages/server/scripts/create-contributor-institutions.mjs
    packages/server/src/db/schema.ts
  </files>
  <action>
Create `packages/server/scripts/create-contributor-institutions.mjs` following the exact pattern from `create-institutions-table.mjs`:
- Import `postgres` from "postgres", load DATABASE_URL from env
- SQL creates `contributor_institutions` table:
  - `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`
  - `contributor_id UUID NOT NULL REFERENCES contributors(id) ON DELETE CASCADE`
  - `institution_id UUID NOT NULL REFERENCES institutions(id) ON DELETE CASCADE`
  - `assigned_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`
  - `assigned_by UUID REFERENCES contributors(id) ON DELETE SET NULL`
  - `CONSTRAINT contributor_institutions_unique UNIQUE (contributor_id, institution_id)`
- Create indexes: `idx_ci_contributor_id ON contributor_institutions(contributor_id)` and `idx_ci_institution_id ON contributor_institutions(institution_id)`
- Use `IF NOT EXISTS` for table and indexes (idempotent)
- After SQL, mark migration in drizzle.__drizzle_migrations using the pattern from `mark-migration.mjs`

In `packages/server/src/db/schema.ts`, add `contributorInstitutions` table definition AFTER the `institutions` export:
- Follow the `circleMembers` pattern exactly (same structure: pgTable with unique constraint in third arg)
- Fields: id (uuid pk), contributorId (uuid FK → contributors.id, cascade), institutionId (uuid FK → institutions.id, cascade), assignedAt (timestamp with tz, defaultNow, notNull), assignedBy (uuid FK → contributors.id, set null)
- Unique constraint: `unique("contributor_institutions_unique").on(table.contributorId, table.institutionId)`
- Export as `contributorInstitutions`

Run the migration script against the local database. Verify with a quick SQL query that the table and indexes exist.

IMPORTANT: This is a many-to-many junction table per user decision — NOT a single FK on the contributors table. The roadmap mentions "institution_id FK on contributors" but CONTEXT.md overrides this.
  </action>
  <verify>
Run `node packages/server/scripts/create-contributor-institutions.mjs` — should complete without error.
Run `npx tsx -e "import { contributorInstitutions } from './packages/server/src/db/schema.js'; console.log(Object.keys(contributorInstitutions))"` or verify TypeScript compiles: `cd packages/server && npx tsc --noEmit` should pass.
  </verify>
  <done>
contributor_institutions table exists in PostgreSQL with id, contributor_id, institution_id, assigned_at, assigned_by columns, composite unique constraint, and both FK indexes. Drizzle schema exports `contributorInstitutions` matching the SQL schema exactly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create shared Zod schemas for institution management</name>
  <files>
    packages/shared/src/schemas/institution.ts
    packages/shared/src/index.ts
  </files>
  <action>
Create `packages/shared/src/schemas/institution.ts` following the pattern from `circle.ts` or `challenge.ts`:

Schemas to define:
1. `createInstitutionSchema` — z.object: name (string, min 2, max 200), description (string, max 2000, optional), city (string, min 2, max 100)
   - Do NOT include slug — it is auto-generated from name on create (per research: slug is immutable after creation)
2. `updateInstitutionSchema` — z.object: name (string, min 2, max 200, optional), description (string, max 2000, optional), city (string, min 2, max 100, optional)
   - Do NOT include slug — slug is immutable (kiosk QR codes depend on it)
3. `toggleActiveSchema` — z.object: isActive (z.boolean())
4. `setContributorInstitutionsSchema` — z.object: institutionIds (z.array(z.string().uuid()))
   - Used for the "set assignments" endpoint (PUT full list in transaction)

Export all schemas from the file. Add re-exports to `packages/shared/src/index.ts`.

Verify the shared package compiles and exports are accessible.
  </action>
  <verify>
`cd packages/shared && npx tsc --noEmit` passes. Importing `{ createInstitutionSchema }` from the shared package resolves correctly.
  </verify>
  <done>
Four Zod schemas exported from shared package: createInstitutionSchema, updateInstitutionSchema, toggleActiveSchema, setContributorInstitutionsSchema. All validation rules match requirements (name length, no slug field on create/edit).
  </done>
</task>

</tasks>

<verification>
1. `contributor_institutions` table exists with correct columns, constraints, and indexes
2. Drizzle schema `contributorInstitutions` export matches SQL table exactly
3. Shared Zod schemas compile and export correctly
4. `npx tsc --noEmit` passes in both server and shared packages
</verification>

<success_criteria>
- Junction table migration runs successfully (idempotent)
- Drizzle schema updated with contributorInstitutions matching circleMembers pattern
- Shared Zod schemas validate institution CRUD payloads
- No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-institution-data-foundation/12-01-SUMMARY.md`
</output>
