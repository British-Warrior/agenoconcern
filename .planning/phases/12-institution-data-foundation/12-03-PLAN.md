---
phase: 12-institution-data-foundation
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - packages/server/src/routes/admin.ts
  - packages/server/src/routes/institutions.ts
  - packages/web/src/api/admin.ts
  - packages/web/src/hooks/useInstitutions.ts
  - packages/web/src/pages/admin/InstitutionManagement.tsx
  - packages/web/src/pages/admin/ContributorDetail.tsx
  - packages/web/src/pages/institution/InstitutionLanding.tsx
  - packages/web/src/App.tsx
autonomous: false

must_haves:
  truths:
    - "CM can assign a contributor to institutions from the contributor detail page via multi-select picker and the contributor appears in the institution's card contributor list"
    - "CM can remove a contributor's institution assignment from the contributor detail page"
    - "CM can view all contributors assigned to a specific institution with name, status, and last activity"
    - "Unassigned contributors appear in a separate 'Unassigned' group in the CM view"
    - "Institution landing page displays live contributor count, challenge count, and hours from real data"
    - "Institution landing page hides stats section when no contributors are assigned"
    - "Institution cards in CM view show the same live stats as the public landing page"
  artifacts:
    - path: "packages/server/src/routes/admin.ts"
      provides: "Contributor assignment endpoints and live stats in institution list"
      contains: "contributor_institutions"
    - path: "packages/server/src/routes/institutions.ts"
      provides: "Updated public endpoint returning live stats"
      contains: "getLiveStats"
    - path: "packages/web/src/pages/admin/InstitutionManagement.tsx"
      provides: "Institution cards with live stats and contributor list"
      contains: "contributorCount|liveStats"
    - path: "packages/web/src/pages/admin/ContributorDetail.tsx"
      provides: "Contributor detail page with multi-select institution picker"
      exports: ["ContributorDetail"]
    - path: "packages/web/src/pages/institution/InstitutionLanding.tsx"
      provides: "Landing page using live stats instead of statsJson"
      contains: "stats"
  key_links:
    - from: "packages/web/src/pages/admin/InstitutionManagement.tsx"
      to: "packages/server/src/routes/admin.ts"
      via: "GET /api/admin/institutions returns live stats per institution"
      pattern: "contributors.*challenges.*hours"
    - from: "packages/web/src/pages/institution/InstitutionLanding.tsx"
      to: "packages/server/src/routes/institutions.ts"
      via: "GET /api/institutions/:slug returns live stats"
      pattern: "stats"
    - from: "packages/web/src/pages/admin/ContributorDetail.tsx"
      to: "packages/web/src/hooks/useInstitutions.ts"
      via: "Multi-select institution picker calls useSetContributorInstitutions"
      pattern: "useSetContributorInstitutions"
    - from: "packages/web/src/pages/admin/InstitutionManagement.tsx"
      to: "packages/web/src/pages/admin/ContributorDetail.tsx"
      via: "Unassigned section links to /admin/contributors/:id"
      pattern: "Link.*admin/contributors"
    - from: "packages/server/src/routes/admin.ts"
      to: "packages/server/src/db/schema.ts"
      via: "queries on contributorInstitutions junction table"
      pattern: "contributorInstitutions"
---

<objective>
Add contributor-institution assignment endpoints, update the CM institution cards with live stats and contributor lists, and replace statsJson with live aggregation on the public landing page.

Purpose: Completes INST-05 through INST-08 — CM can manage contributor assignments and both CM and public views show live aggregated stats from real contributor data.
Output: Full contributor assignment workflow, live stats on CM cards and public landing page.
</objective>

<execution_context>
@C:\Users\kirk_\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\kirk_\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-institution-data-foundation/12-RESEARCH.md
@.planning/phases/12-institution-data-foundation/12-CONTEXT.md
@.planning/phases/12-institution-data-foundation/12-01-SUMMARY.md
@.planning/phases/12-institution-data-foundation/12-02-SUMMARY.md
@packages/server/src/routes/admin.ts
@packages/server/src/routes/institutions.ts
@packages/server/src/routes/circles.ts
@packages/server/src/db/schema.ts
@packages/web/src/pages/admin/InstitutionManagement.tsx
@packages/web/src/pages/institution/InstitutionLanding.tsx
@packages/web/src/api/admin.ts
@packages/web/src/hooks/useInstitutions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add contributor assignment endpoints and live stats to admin routes</name>
  <files>
    packages/server/src/routes/admin.ts
    packages/server/src/routes/institutions.ts
  </files>
  <action>
**In `packages/server/src/routes/admin.ts`**, add these endpoints:

1. `GET /institutions/:id/contributors` — List contributors for an institution. Join `contributorInstitutions` with `contributors` table. Return: id, name (firstName + lastName), role (as "platform status"), lastActivity (see below). Order by name. Validate UUID param.

2. `PUT /contributors/:contributorId/institutions` — **Set assignments** (per user decision: assignment from contributor profile). Validate body with `setContributorInstitutionsSchema` from shared package. Wrap in `db.transaction`: DELETE all existing rows for contributorId, then INSERT new rows for each institutionId (with assignedBy = req.contributor.id). Guard against empty institutionIds array (just delete all = unassign from everything, which is valid). Return 200 with updated list.

3. `GET /contributors` — List all contributors with their institution assignments. Return: id, firstName, lastName, role, institutions (array of {id, name}). This powers the "Unassigned" group in the CM view — contributors with empty institutions array are unassigned.

**Last activity resolution** (Claude's discretion): For each contributor in the institution contributor list, compute last activity as the more recent of:
- MAX(wellbeing_checkins.completed_at) for that contributor
- MAX(circle_notes.created_at) where author_id = contributor
Use two batch queries (not N+1): one for all memberIds against wellbeingCheckins, one against circleNotes, both using `inArray`. Merge in TypeScript taking the later date. Return as ISO string or null ("No activity yet").

**CRITICAL: Guard against empty `inArray`** — if memberIds is empty, skip aggregate queries and return empty array. `inArray(col, [])` generates invalid SQL.

**Batch live stats for institution list**: Update `GET /institutions` to include live stats per institution. Query all `contributorInstitutions` rows in one pass, group by institutionId in TypeScript. For each group, count contributors. Then batch-query challengeInterests and contributorHours for all assigned contributor IDs (deduplicated across institutions). Compute per-institution: contributorCount, challengeCount, hoursTotal. Return these alongside institution fields in the response.

**In `packages/server/src/routes/institutions.ts`**, update the public `GET /api/institutions/:slug` endpoint:
- After fetching the institution record, compute live stats using the same aggregation pattern (get contributor IDs from junction table, aggregate challenges and hours)
- Return `stats: liveStats.contributors > 0 ? liveStats : null` (per user decision: hide stats section when no contributors assigned — null signals this to the frontend)
- Keep statsJson in the DB (per user decision: cache/fallback) but do NOT return it to the client — live stats replace it
- CRITICAL: Guard `inArray` against empty array
  </action>
  <verify>
`cd packages/server && npx tsc --noEmit` passes.
Test with curl:
- GET /api/admin/institutions returns institutions with stats fields
- PUT /api/admin/contributors/:id/institutions with institutionIds updates assignments
- GET /api/admin/institutions/:id/contributors returns contributor list with lastActivity
- GET /api/admin/contributors returns all contributors with institution arrays
- GET /api/institutions/:slug returns live stats (or null if no contributors)
  </verify>
  <done>
Contributor assignment endpoints operational (set assignments via PUT, list contributors per institution, list all contributors with assignments). Live stats computed per institution in batch (no N+1). Public landing endpoint returns live aggregated stats instead of statsJson. Empty inArray guards in place.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CM institution cards with live stats and contributor list, update landing page</name>
  <files>
    packages/web/src/api/admin.ts
    packages/web/src/hooks/useInstitutions.ts
    packages/web/src/pages/admin/InstitutionManagement.tsx
    packages/web/src/pages/institution/InstitutionLanding.tsx
  </files>
  <action>
**admin.ts (API client)** — Add functions:
- `getInstitutionContributors(id)` → GET /api/admin/institutions/:id/contributors
- `setContributorInstitutions(contributorId, institutionIds)` → PUT /api/admin/contributors/:contributorId/institutions
- `getAllContributors()` → GET /api/admin/contributors

**useInstitutions.ts** — Add hooks:
- `useInstitutionContributors(institutionId)` — useQuery with key ["admin", "institutions", id, "contributors"], enabled only when id is truthy
- `useSetContributorInstitutions()` — useMutation, invalidates ["admin", "institutions"] and ["admin", "contributors"] on success
- `useAllContributors()` — useQuery with key ["admin", "contributors"]

**InstitutionManagement.tsx** — Update institution cards:
- Each card now shows live stats below the name/city/badge: "X contributors · Y challenges · Z hours" (data comes from updated GET /institutions response)
- Per user decision: same stats visible on CM cards as on public page
- Per user decision: if stats are all zeros, show "No contributors assigned" instead
- Add expandable contributor list on each card: click to expand shows list of assigned contributors with name, platform status (role), and last activity date (formatted relative — "3 days ago" or "No activity yet")
- Add "Unassigned" section at the bottom of the page: list contributors from `useAllContributors()` who have empty institutions array. Show as a simple list with name and a "View" link. Each link navigates to `/admin/contributors/:id` (the contributor detail page where the actual assignment picker lives, per user decision: assignment direction from contributor profile). Do NOT embed the multi-select picker on this page — it lives on the contributor detail page.

**InstitutionLanding.tsx** — Update to use live stats:
- The API now returns `stats: { contributors, challenges, hours } | null`
- If `stats` is null, hide the stats section entirely (per user decision: don't show zeros)
- If `stats` is present, display contributor count, active challenge count, total hours
- Remove any references to `statsJson` — the live stats field replaces it
- Keep the same visual presentation as before (same layout, same metrics)
  </action>
  <verify>
`cd packages/web && npx tsc --noEmit` passes.
Start dev server, log in as CM:
- Institution cards show live stats (contributor count, challenges, hours)
- Expanding a card shows contributor list with last activity
- "Unassigned" section shows contributors not linked to any institution with "View" links
- Clicking "View" on unassigned contributor navigates to /admin/contributors/:id
- Public landing page (/i/:slug) shows live stats when contributors assigned
- Public landing page hides stats section when no contributors assigned
  </verify>
  <done>
CM institution cards display live stats and expandable contributor list. Unassigned contributors shown in separate group with links to contributor detail pages. Public landing page displays live aggregated stats and hides stats section when empty.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create contributor detail page with multi-select institution picker</name>
  <files>
    packages/web/src/pages/admin/ContributorDetail.tsx
    packages/web/src/App.tsx
  </files>
  <action>
**ContributorDetail.tsx** — New page at route `/admin/contributors/:id` (per user decision: assignment direction from the contributor profile).

- Read `id` from route params via `useParams`
- Fetch contributor data using `useAllContributors()` hook and find by id (or add a dedicated `useContributor(id)` query if preferred — Claude's discretion)
- Display contributor info at top: name (firstName + lastName), role/platform status
- **Multi-select institution picker** (the core of this task, per locked user decision: "from the contributor profile — CM selects institutions via a multi-select picker (checkboxes/tags showing all institutions)"):
  - Fetch all institutions via `useInstitutions()` hook
  - Render each institution as a checkbox row: checkbox + institution name + city + active badge
  - Pre-check institutions the contributor is currently assigned to (from contributor's institutions array)
  - "Save Assignments" button calls `useSetContributorInstitutions` with the full list of checked institution IDs
  - "Cancel" button or back link navigates back to `/admin/institutions`
  - Show toast or inline success message on save
- Use existing UI components: Card, Button, Input from `components/ui/`
- Use the `KioskWarningOverlay` overlay pattern if implementing as modal, OR render inline on the page (Claude's discretion — inline is simpler and preferred)
- Style with Tailwind: clean layout, clear visual grouping of contributor info vs assignment picker

**App.tsx** — Add route inside the `<CMRoute>` guard:
```tsx
<Route path="/admin/contributors/:id" element={<ContributorDetail />} />
```
Import `ContributorDetail` from the new page file.
  </action>
  <verify>
`cd packages/web && npx tsc --noEmit` passes.
Start dev server, log in as CM:
- Navigate to /admin/contributors/:id for a known contributor
- Verify contributor name and role displayed
- Verify all institutions shown as checkboxes
- Verify currently assigned institutions are pre-checked
- Check/uncheck institutions and click Save — verify assignments update
- Navigate back to /admin/institutions — verify contributor appears in correct institution cards
  </verify>
  <done>
Contributor detail page at /admin/contributors/:id displays contributor info and multi-select institution picker with checkboxes for all institutions. Saving updates assignments via PUT endpoint. Route registered in App.tsx under CMRoute guard. Assignment direction is from the contributor profile per locked user decision.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete contributor-institution assignment workflow, live stats on CM cards and public landing page, unassigned contributors group with links, contributor detail page with multi-select institution picker.</what-built>
  <how-to-verify>
1. Start the dev server (`npm run dev` in root)
2. Log in as community_manager
3. Navigate to /admin/institutions
4. Verify each institution card shows live stats (contributors, challenges, hours)
5. Click an institution card to expand — verify contributor list shows names, status, last activity
6. Scroll down — verify "Unassigned" section shows contributors without institution links
7. Click "View" on an unassigned contributor — verify navigation to /admin/contributors/:id
8. On the contributor detail page, verify contributor name and role displayed
9. Verify all institutions shown as checkboxes in the multi-select picker
10. Check two institutions and save — verify success feedback
11. Navigate back to /admin/institutions — verify contributor moved out of "Unassigned" and into both institution cards
12. Click "View" on the same contributor again — verify both institutions are pre-checked
13. Uncheck one institution and save — navigate back — verify contributor in one card only
14. Open a new browser tab, navigate to /i/{slug} for an institution with assigned contributors
15. Verify live stats display (contributor count, challenges, hours) — should match CM card numbers
16. Navigate to /i/{slug} for an institution with NO assigned contributors
17. Verify stats section is hidden (no zeros displayed)
18. Check that institution card stats update after assignment changes (refresh page if needed)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Contributor assignment endpoints work (set, list per institution, list all with assignments)
2. Live stats computed via batch queries (no N+1) for both CM list and public landing
3. Empty inArray guards prevent SQL errors on institutions with no contributors
4. CM cards and public landing page show same live stats
5. Stats hidden when no contributors assigned (null stats)
6. Unassigned contributors visible in CM view with links to contributor detail pages
7. Multi-select picker on contributor detail page (/admin/contributors/:id) allows many-to-many assignment
8. Contributor detail page route registered in App.tsx under CMRoute guard
9. TypeScript compiles in both server and web packages
</verification>

<success_criteria>
- INST-05: CM can assign a contributor to institutions via multi-select picker on contributor detail page (/admin/contributors/:id)
- INST-06: CM can remove a contributor's institution assignment from the contributor detail page
- INST-07: CM can view all contributors assigned to a specific institution with name, status, last activity
- INST-08: Institution landing page displays live aggregated stats (contributor count, challenges, hours) instead of static JSONB
- Unassigned contributors shown in separate group (per user decision)
- Stats hidden when no contributors assigned (per user decision)
- statsJson column preserved as cache/fallback (per user decision, not dropped)
</success_criteria>

<output>
After completion, create `.planning/phases/12-institution-data-foundation/12-03-SUMMARY.md`
</output>
